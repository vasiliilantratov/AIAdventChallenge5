# Примеры использования новой функциональности

Этот документ содержит практические примеры использования команды `chat` и MCP для релиза приложения.

## 1. Базовый интерактивный чат

### Запуск

```bash
./gradlew run --args='chat'
```

### Пример сессии

```
╔══════════════════════════════════════════════════════════════╗
║              Чат с LLM - Интерактивный режим                 ║
╚══════════════════════════════════════════════════════════════╝

Доступные команды:
  • Любой вопрос - общайтесь с LLM
  • 'зарелизь приложение', 'сделай релиз' - запуск релиза
  • '/exit', '/quit' - выход из чата
  • '/clear' - начать новую беседу
  • '/history' - показать историю текущей беседы

👤 Вы: Привет! Как дела?

🤖 Ассистент: Привет! У меня всё отлично, спасибо! Я готов помочь тебе с вопросами или выполнить задачи. Что тебя интересует?

----------------------------------------------------------------

👤 Вы: Расскажи мне про Kotlin корутины

🤖 Ассистент: Kotlin корутины — это легковесные потоки, которые позволяют писать асинхронный код в синхронном стиле. Они работают на основе suspend функций и позволяют приостанавливать выполнение без блокировки потока. Корутины управляются корутинным контекстом и диспетчерами (Dispatchers), которые определяют, на каком потоке будет выполняться код.

----------------------------------------------------------------

👤 Вы: /history

═══════════════════════════════════════════════════════════════
История беседы #1
═══════════════════════════════════════════════════════════════

👤 Вы (chat):
Привет! Как дела?

🤖 Ассистент (chat):
Привет! У меня всё отлично, спасибо! Я готов помочь тебе...

----------------------------------------------------------------

👤 Вы (chat):
Расскажи мне про Kotlin корутины

🤖 Ассистент (chat):
Kotlin корутины — это легковесные потоки...

----------------------------------------------------------------

👤 Вы: /exit

Завершение чата. До встречи!
```

## 2. Чат с начальным сообщением

Можно запустить чат сразу с первым сообщением:

```bash
./gradlew run --args='chat Привет! Расскажи мне про функциональное программирование'
```

Это полезно для быстрых одноразовых запросов.

## 3. Релиз приложения через чат

### Способ 1: Интерактивный режим

```bash
./gradlew run --args='chat'

# В чате:
👤 Вы: зарелизь приложение
```

### Способ 2: Прямой запрос

```bash
./gradlew run --args='chat зарелизь приложение'
```

### Пример успешного релиза

```
👤 Вы: зарелизь приложение

🚀 Обнаружен запрос на релиз приложения...

   1. Проверка локальной директории... ✅
   2. Проверка SSH соединения с my_mon_bot... ✅
   3. Получение списка файлов... найдено 15 файлов
   
   Файлы для загрузки:
      • main.py
      • config.py
      • requirements.txt
      • handlers/
      • utils.py
      • database.py
      • models.py
      • bot.py
      • .env.example
      • README.md
   
   4. Загрузка файлов на сервер... 

✅ Релиз успешно завершен!

📊 Статистика:
   • Загружено файлов: 15
   • Время выполнения: 3.2 секунд
   • Удаленная директория: /root/release на my_mon_bot

📦 Загруженные файлы:
   • main.py
   • config.py
   • requirements.txt
   • handlers/__init__.py
   • handlers/start.py
   • handlers/message.py
   • utils.py
   • database.py
   • models.py
   • bot.py
   • .env.example
   • README.md
   • Dockerfile
   • docker-compose.yml
   • .gitignore

📁 Содержимое удаленной директории:
   total 68K
   drwxr-xr-x 4 root root 4.0K Jan 17 14:23 .
   drwx------ 8 root root 4.0K Jan 17 14:20 ..
   -rw-r--r-- 1 root root  123 Jan 17 14:23 .gitignore
   -rw-r--r-- 1 root root  456 Jan 17 14:23 config.py
   ...
```

## 4. Различные варианты запросов на релиз

LLM использует интеллектуальный анализ текста для определения намерения, поэтому понимает не только прямые команды, но и косвенные формулировки:

```bash
# Прямые команды
./gradlew run --args='chat зарелизь приложение'
./gradlew run --args='chat сделай релиз'
./gradlew run --args='chat выполни релиз'

# Вежливые просьбы
./gradlew run --args='chat можешь зарелизить приложение?'
./gradlew run --args='chat пожалуйста, сделай деплой'

# Естественные формулировки
./gradlew run --args='chat пора делать релиз'
./gradlew run --args='chat давай зарелизим'
./gradlew run --args='chat нужно загрузить файлы на сервер'

# Английский язык
./gradlew run --args='chat deploy the application'
./gradlew run --args='chat please make a release'

# Контекстные запросы
./gradlew run --args='chat всё готово, можно релизить'
```

**Что НЕ сработает** (вопросы, а не команды):
```bash
./gradlew run --args='chat что такое релиз?'
./gradlew run --args='chat как работает деплой?'
./gradlew run --args='chat когда был последний релиз?'
./gradlew run --args='chat нужно ли делать релиз?'
```

LLM понимает разницу между командой и вопросом!

## 5. Комбинированные запросы

Можно задавать вопросы и выполнять релиз в одной сессии:

```bash
./gradlew run --args='chat'

# В чате:
👤 Вы: какие файлы есть в проекте EchoBot?
🤖 Ассистент: [отвечает на вопрос]

👤 Вы: хорошо, теперь зарелизь это на сервер
🤖 Ассистент: [выполняет релиз]

👤 Вы: проверь что всё загрузилось
🤖 Ассистент: [отвечает на вопрос]
```

## 6. Работа с историей

### Просмотр текущей беседы

```bash
# В интерактивном чате:
👤 Вы: /history
```

### Просмотр всех бесед

```bash
./gradlew run --args='history'
```

### Просмотр конкретной беседы

```bash
./gradlew run --args='history --conversation-id 1 --format full'
```

## 7. Начало новой беседы

Если нужно начать новый разговор с чистого листа:

```bash
# В интерактивном чате:
👤 Вы: /clear

✨ Начата новая беседа (ID: 2)
```

## 8. Обработка ошибок

### Ошибка: SSH недоступен

```
👤 Вы: зарелизь приложение

🚀 Обнаружен запрос на релиз приложения...

   1. Проверка локальной директории... ✅
   2. Проверка SSH соединения с my_mon_bot... ❌

❌ Ошибка: не удалось подключиться к серверу через SSH (my_mon_bot).

Проверьте:
1. SSH конфиг в ~/.ssh/config
2. Доступность сервера
3. SSH ключи
```

### Ошибка: Директория не найдена

```
👤 Вы: зарелизь приложение

🚀 Обнаружен запрос на релиз приложения...

   1. Проверка локальной директории... ❌

❌ Ошибка: локальная директория /home/vas/Documents/Projects/EchoBot не найдена.

Проверьте:
1. Путь к директории
2. Наличие файлов для релиза
```

## 9. Программное использование MCP

Если нужно использовать MCP напрямую в коде:

```kotlin
import org.example.mcp.ReleaseMcp
import kotlinx.coroutines.runBlocking

fun main() = runBlocking {
    val mcp = ReleaseMcp(
        sshConfig = "my_mon_bot",
        localDir = "/home/vas/Documents/Projects/EchoBot",
        remoteDir = "/root/release"
    )
    
    // Проверяем соединение
    println("Проверка SSH...")
    if (!mcp.testConnection()) {
        println("Ошибка: SSH недоступен")
        return@runBlocking
    }
    
    // Получаем список файлов
    println("Получение списка файлов...")
    val files = mcp.getLocalFiles()
    println("Найдено файлов: ${files.size}")
    
    // Выполняем релиз
    println("Запуск релиза...")
    val result = mcp.release()
    
    if (result.success) {
        println("✅ Релиз успешен!")
        println("Загружено: ${result.uploadedFiles.size} файлов")
        println("Время: ${result.durationMs / 1000.0} сек")
        
        // Получаем информацию о файлах на сервере
        val remoteInfo = mcp.getRemoteInfo()
        println("\nФайлы на сервере:")
        println(remoteInfo)
    } else {
        println("❌ Ошибка релиза: ${result.message}")
        result.errors.forEach { 
            println("  - $it") 
        }
    }
}
```

## 10. Интеграция с другими командами

Команда `chat` хорошо сочетается с другими командами проекта:

```bash
# Сначала индексируем документацию
./gradlew run --args='index /path/to/docs'

# Теперь можем задавать вопросы в чате
./gradlew run --args='chat'

👤 Вы: найди информацию о настройке проекта
🤖 Ассистент: [использует RAG для поиска в документации]

👤 Вы: теперь зарелизь проект на сервер
🤖 Ассистент: [выполняет релиз через MCP]
```

## 11. Автоматизация через скрипты

Можно создать скрипты для автоматизации:

### deploy.sh

```bash
#!/bin/bash

echo "Автоматический deploy..."

# Запускаем релиз через chat
./gradlew run --args='chat зарелизь приложение' | tee deploy.log

# Проверяем успешность
if grep -q "✅ Релиз успешно завершен" deploy.log; then
    echo "Deploy успешен!"
    # Можно добавить дополнительные действия
    # например, перезапуск сервиса на сервере
    ssh my_mon_bot "systemctl restart myapp"
else
    echo "Deploy провалился!"
    exit 1
fi
```

### check-and-deploy.sh

```bash
#!/bin/bash

echo "Проверка и deploy..."

# Проверяем тесты
echo "Запуск тестов..."
./gradlew test

if [ $? -eq 0 ]; then
    echo "Тесты прошли успешно, запускаем deploy..."
    ./gradlew run --args='chat зарелизь приложение'
else
    echo "Тесты провалились, deploy отменен!"
    exit 1
fi
```

## 12. CI/CD интеграция

Пример использования в GitHub Actions:

```yaml
name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up Java
      uses: actions/setup-java@v2
      with:
        java-version: '21'
        
    - name: Setup SSH
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        echo "${{ secrets.SSH_CONFIG }}" > ~/.ssh/config
        
    - name: Build project
      run: ./gradlew build
      
    - name: Deploy
      run: |
        ./gradlew run --args='chat зарелизь приложение'
```

## Полезные советы

1. **Используйте /history** для отслеживания всех выполненных релизов
2. **Создавайте резервные копии** перед релизом на production
3. **Тестируйте на staging** перед production релизом
4. **Сохраняйте логи** релизов для отладки
5. **Автоматизируйте проверки** после релиза

## Заключение

Команда `chat` с интегрированным MCP для релиза предоставляет удобный интерфейс для:
- Общения с LLM
- Автоматического релиза приложений
- Отслеживания истории операций
- Интеграции в существующие процессы разработки

Для получения дополнительной информации см.:
- `README.md` - основная документация
- `RELEASE_MCP.md` - детальная документация по MCP релиза
- `./test-new-features.sh` - скрипт проверки работоспособности
