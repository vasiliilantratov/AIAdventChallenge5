# MCP Release System - Документация

## Описание

MCP (Model Context Protocol) клиент для автоматизации релиза приложений на удаленный сервер через SSH.

## Архитектура

### Класс ReleaseMcp

Основной класс для управления релизом приложения.

**Параметры конструктора:**
- `sshConfig: String` - имя SSH конфигурации (по умолчанию: "my_mon_bot")
- `localDir: String` - путь к локальной директории с файлами (по умолчанию: "/home/vas/Documents/Projects/EchoBot")
- `remoteDir: String` - путь к удаленной директории на сервере (по умолчанию: "/root/release")

### Основные методы

#### testConnection(): Boolean
Проверяет доступность SSH соединения с сервером.

**Возвращает:** `true` если соединение успешно, `false` иначе

**Пример:**
```kotlin
val releaseMcp = ReleaseMcp()
if (releaseMcp.testConnection()) {
    println("SSH соединение успешно")
}
```

#### checkLocalDirectory(): Boolean
Проверяет существование локальной директории с файлами для релиза.

**Возвращает:** `true` если директория существует, `false` иначе

#### getLocalFiles(): List<String>
Получает список всех файлов в локальной директории (рекурсивно).

**Возвращает:** список относительных путей к файлам

**Пример:**
```kotlin
val files = releaseMcp.getLocalFiles()
println("Найдено ${files.size} файлов:")
files.forEach { println("  - $it") }
```

#### createRemoteDirectory(): String
Создает удаленную директорию на сервере, если её нет.

**Возвращает:** сообщение о результате операции

#### release(): ReleaseResult
Выполняет полный процесс релиза: проверка условий, создание директории, синхронизация файлов.

**Возвращает:** объект `ReleaseResult` с детальной информацией о результате

**Пример:**
```kotlin
val result = releaseMcp.release()
if (result.success) {
    println("Релиз успешен! Загружено ${result.uploadedFiles.size} файлов")
    println("Время выполнения: ${result.durationMs / 1000.0} секунд")
} else {
    println("Ошибка релиза: ${result.message}")
    result.errors.forEach { println("  - $it") }
}
```

#### getRemoteInfo(): String
Получает информацию о содержимом удаленной директории.

**Возвращает:** вывод команды `ls -lah` для удаленной директории

#### cleanRemoteDirectory(): String
Очищает содержимое удаленной директории на сервере.

**Возвращает:** сообщение о результате операции

**⚠️ Внимание:** эта операция удаляет все файлы в удаленной директории!

### Класс ReleaseResult

Результат выполнения релиза.

**Поля:**
- `success: Boolean` - флаг успешности релиза
- `message: String` - основное сообщение о результате
- `uploadedFiles: List<String>` - список загруженных файлов
- `errors: List<String>` - список ошибок (если есть)
- `durationMs: Long` - время выполнения в миллисекундах

## Интеграция с Chat Command

Команда `chat` автоматически определяет намерение пользователя выполнить релиз на основе ключевых слов:

**Ключевые слова для определения релиза:**
- зарелиз, зарелизить
- сделай релиз, выполни релиз, запусти релиз
- нужно зарелизить, нужен релиз
- загрузи на сервер
- deploy, деплой

**Процесс выполнения релиза в chat:**

1. **Обнаружение намерения**
   ```
   👤 Вы: зарелизь приложение
   🚀 Обнаружен запрос на релиз приложения...
   ```

2. **Проверка условий**
   ```
   1. Проверка локальной директории... ✅
   2. Проверка SSH соединения с my_mon_bot... ✅
   3. Получение списка файлов... найдено 15 файлов
   ```

3. **Загрузка файлов**
   ```
   4. Загрузка файлов на сервер...
   ```

4. **Результат**
   ```
   ✅ Релиз успешно завершен!
   
   📊 Статистика:
      • Загружено файлов: 15
      • Время выполнения: 3.2 секунд
      • Удаленная директория: /root/release на my_mon_bot
   
   📦 Загруженные файлы:
      • main.py
      • config.py
      • requirements.txt
      ...
   ```

## Настройка SSH

### Требования

1. **SSH-ключ для беспарольного входа**

   Сгенерируйте SSH-ключ, если у вас его еще нет:
   ```bash
   ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
   ```

2. **Копирование ключа на сервер**
   ```bash
   ssh-copy-id root@your-server.com
   ```

3. **Настройка SSH конфига**

   Отредактируйте файл `~/.ssh/config`:
   ```
   Host my_mon_bot
       HostName your-server.com
       User root
       Port 22
       IdentityFile ~/.ssh/id_rsa
       StrictHostKeyChecking no
       UserKnownHostsFile=/dev/null
   ```

4. **Проверка подключения**
   ```bash
   ssh my_mon_bot "echo OK"
   ```
   
   Должно вывести: `OK`

### Установка rsync

**На локальной машине (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install rsync
```

**На сервере (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install rsync
```

**Для других ОС:**
- macOS: rsync уже установлен
- Windows: используйте WSL или Cygwin

## Безопасность

### Ограничения доступа

1. **SSH ключи**: Используйте SSH-ключи вместо паролей для безопасного доступа
2. **Права доступа**: Убедитесь, что пользователь имеет права только на необходимые директории
3. **Ограничение команд**: В SSH конфиге можно ограничить доступные команды через `command=`

### Проверка путей

Класс `ReleaseMcp` выполняет проверки:
- Существование локальной директории
- Доступность SSH соединения перед выполнением операций
- Корректность путей к файлам

### Логирование

Все операции релиза логируются:
- В консоль (детальный вывод процесса)
- В историю чата (сохранение в базу данных)
- Результат с метаинформацией (количество файлов, время, ошибки)

## Использование в коде

### Базовый пример

```kotlin
import org.example.mcp.ReleaseMcp

fun main() {
    val releaseMcp = ReleaseMcp()
    
    // Проверяем соединение
    if (!releaseMcp.testConnection()) {
        println("Ошибка: SSH соединение недоступно")
        return
    }
    
    // Получаем список файлов
    val files = releaseMcp.getLocalFiles()
    println("Будет загружено ${files.size} файлов")
    
    // Выполняем релиз
    val result = releaseMcp.release()
    
    if (result.success) {
        println("✅ Релиз успешен!")
        println("Время: ${result.durationMs / 1000.0} сек")
    } else {
        println("❌ Ошибка релиза: ${result.message}")
    }
}
```

### Кастомная конфигурация

```kotlin
val customReleaseMcp = ReleaseMcp(
    sshConfig = "my_custom_server",
    localDir = "/path/to/my/app",
    remoteDir = "/var/www/app"
)

val result = customReleaseMcp.release()
```

### Проверка перед релизом

```kotlin
val releaseMcp = ReleaseMcp()

// Шаг 1: Проверка локальной директории
if (!releaseMcp.checkLocalDirectory()) {
    println("Локальная директория не найдена")
    return
}

// Шаг 2: Проверка SSH
if (!releaseMcp.testConnection()) {
    println("SSH соединение недоступно")
    return
}

// Шаг 3: Проверка файлов
val files = releaseMcp.getLocalFiles()
if (files.isEmpty()) {
    println("Нет файлов для загрузки")
    return
}

// Шаг 4: Релиз
val result = releaseMcp.release()
println(if (result.success) "Успех" else "Ошибка")
```

## Troubleshooting

### Проблема: SSH соединение не работает

**Решение:**
1. Проверьте SSH конфиг: `cat ~/.ssh/config`
2. Проверьте подключение: `ssh my_mon_bot "echo OK"`
3. Проверьте права на ключ: `chmod 600 ~/.ssh/id_rsa`
4. Проверьте, что сервер в known_hosts: `ssh-keyscan -H your-server.com >> ~/.ssh/known_hosts`

### Проблема: rsync не установлен

**Решение:**
```bash
# Ubuntu/Debian
sudo apt-get install rsync

# CentOS/RHEL
sudo yum install rsync

# macOS (обычно уже установлен)
brew install rsync
```

### Проблема: Нет прав на запись в удаленную директорию

**Решение:**
1. Проверьте права на сервере:
   ```bash
   ssh my_mon_bot "ls -la /root"
   ```

2. Создайте директорию с правильными правами:
   ```bash
   ssh my_mon_bot "mkdir -p /root/release && chmod 755 /root/release"
   ```

### Проблема: Файлы не загружаются

**Решение:**
1. Проверьте, что локальная директория существует и содержит файлы
2. Проверьте вывод rsync в консоли для деталей ошибки
3. Проверьте права доступа к локальным файлам: `ls -la /home/vas/Documents/Projects/EchoBot`

## Примеры использования

### 1. Простой релиз через chat

```bash
./gradlew run --args='chat "зарелизь приложение"'
```

### 2. Интерактивный чат с релизом

```bash
./gradlew run --args='chat'

# В интерактивном режиме:
👤 Вы: сделай релиз
```

### 3. Проверка состояния сервера

```bash
./gradlew run --args='chat "проверь что на сервере, потом зарелизь"'
```

### 4. Программное использование

```kotlin
import org.example.mcp.ReleaseMcp

suspend fun performRelease() {
    val mcp = ReleaseMcp()
    
    // Получаем информацию о текущем состоянии
    val remoteInfo = mcp.getRemoteInfo()
    println("Текущее состояние сервера:\n$remoteInfo")
    
    // Выполняем релиз
    val result = mcp.release()
    
    if (result.success) {
        println("✅ Релиз завершен успешно")
        println("Загружено файлов: ${result.uploadedFiles.size}")
        println("Время: ${result.durationMs}ms")
        
        // Получаем обновленную информацию
        val newInfo = mcp.getRemoteInfo()
        println("Новое состояние сервера:\n$newInfo")
    } else {
        println("❌ Ошибка релиза")
        result.errors.forEach { println("  - $it") }
    }
}
```

## Лучшие практики

1. **Всегда проверяйте соединение** перед релизом:
   ```kotlin
   if (!releaseMcp.testConnection()) {
       // обработка ошибки
   }
   ```

2. **Логируйте результаты релиза** для отладки:
   ```kotlin
   val result = releaseMcp.release()
   logger.info("Release result: success=${result.success}, files=${result.uploadedFiles.size}")
   ```

3. **Используйте резервные копии** перед релизом:
   ```bash
   ssh my_mon_bot "tar -czf /root/backup-$(date +%Y%m%d-%H%M%S).tar.gz /root/release"
   ```

4. **Тестируйте на staging** перед production релизом:
   ```kotlin
   val stagingMcp = ReleaseMcp(
       sshConfig = "staging_server",
       remoteDir = "/var/www/staging"
   )
   ```

5. **Автоматизируйте проверки** после релиза:
   ```bash
   ssh my_mon_bot "cd /root/release && python3 -m pytest"
   ```

## FAQ

**Q: Можно ли изменить директории для релиза?**

A: Да, передайте параметры в конструктор:
```kotlin
val mcp = ReleaseMcp(
    localDir = "/path/to/your/app",
    remoteDir = "/path/on/server"
)
```

**Q: Как откатить релиз?**

A: Используйте резервную копию:
```bash
ssh my_mon_bot "rm -rf /root/release && tar -xzf /root/backup-*.tar.gz"
```

**Q: Поддерживаются ли другие протоколы кроме SSH?**

A: В текущей версии поддерживается только SSH через rsync. Для других протоколов можно расширить класс `ReleaseMcp`.

**Q: Можно ли запустить команды после релиза?**

A: Да, используйте SSH:
```kotlin
val remoteCmd = "ssh my_mon_bot 'cd /root/release && ./start.sh'"
Runtime.getRuntime().exec(remoteCmd)
```

## Roadmap

Планируемые улучшения:

- [ ] Поддержка множественных серверов (параллельный deploy)
- [ ] Автоматические резервные копии перед релизом
- [ ] Rollback функциональность
- [ ] Веб-интерфейс для управления релизами
- [ ] Интеграция с CI/CD системами
- [ ] Webhook уведомления о статусе релиза
- [ ] Поддержка Docker контейнеров
- [ ] Blue-green deployment

## Лицензия

Часть проекта Semantic Search Application.
