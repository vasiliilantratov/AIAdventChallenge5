# Semantic Search Application

Консольное приложение на Kotlin для семантического поиска по локальным документам с использованием Ollama и SQLite. Включает:
- **Ассистента разработчика** с поддержкой RAG (Retrieval-Augmented Generation) и безопасного MCP-доступа к репозиторию
- **Командного ассистента** для управления задачами, анализа статуса проекта и рекомендаций по приоритизации

## Требования

- Java 21+
- Gradle
- Ollama с установленной моделью `nomic-embed-text:latest`

### Установка Ollama модели

```bash
ollama pull nomic-embed-text:latest
```

## Сборка

```bash
./gradlew build
```

## Использование

### Индексация директории

```bash
./gradlew run --args="index /path/to/documents --chunk-size 512 --overlap 50"
```

Параметры:
- `--chunk-size`: Размер чанка в символах (по умолчанию: 512)
- `--overlap`: Размер overlap между чанками (по умолчанию: 50)
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)
- `--db-path`: Путь к файлу SQLite (по умолчанию: ./index.db)
- `--exclude`: Паттерн исключения (glob формат, можно использовать несколько раз)
- `--use-default-excludes`: Использовать дефолтные паттерны исключения (по умолчанию: включено)
- `--use-gitignore`: Читать и использовать .gitignore файл, если он есть (по умолчанию: включено)

**Исключение файлов и директорий:**

По умолчанию из индексации исключаются:
- Системные директории: `.git/`, `.svn/`, `.hg/`
- Зависимости: `node_modules/`, `vendor/`, `.venv/`, `venv/`, `env/`, `__pycache__/`
- Артефакты сборки: `build/`, `dist/`, `target/`, `out/`, `bin/`, `obj/`, `.gradle/`
- IDE директории: `.idea/`, `.vscode/`, `.vs/`
- Временные файлы: `.cache/`, `.tmp/`, `tmp/`, `temp/`, `*.tmp`, `*.log`, `*.swp`
- Бинарные файлы: `*.class`, `*.jar`, `*.zip`, `*.so`, `*.dll`, `*.exe` и т.д.
- Медиа файлы: `*.jpg`, `*.png`, `*.mp3`, `*.mp4` и т.д.

**Примеры использования исключений:**

```bash
# Индексация с дополнительными исключениями
./gradlew run --args='index /path/to/project --exclude "*.min.js" --exclude "coverage/**"'

# Индексация без дефолтных исключений (индексировать всё)
./gradlew run --args="index /path/to/project --no-use-default-excludes"

# Индексация без использования .gitignore
./gradlew run --args="index /path/to/project --no-use-gitignore"

# Комбинация: дефолтные исключения + .gitignore + дополнительные
./gradlew run --args='index /path/to/project --exclude "*.test.ts" --exclude "**/__tests__/**"'
```

**Поддержка .gitignore:**

Если в корне индексируемой директории есть файл `.gitignore`, он автоматически читается и паттерны из него применяются как исключения (если включен флаг `--use-gitignore`).

### Поиск

```bash
./gradlew run --args="search \"ваш запрос\" --top-k 10"
```

Параметры:
- `--top-k`: Количество результатов (по умолчанию: 10)
- `--ollama-url`: URL Ollama сервера
- `--db-path`: Путь к файлу SQLite

### Статистика индекса

```bash
./gradlew run --args="stats"
```

### Очистка индекса

```bash
./gradlew run --args="clear"
```

### Удаление документа из индекса

```bash
./gradlew run --args="remove /path/to/file.md"
```

### Вопрос к LLM (режимы с RAG / без RAG)

Команда `qa` отправляет вопрос в LLM в одном из двух режимов. **Все вопросы и ответы автоматически сохраняются в истории диалога.**

```bash
# Без RAG (модель отвечает без учёта индекса)
./gradlew run --args='qa --mode plain Что такое проект Соната-2041?'

# С RAG (поиск релевантных чанков + ответ по контексту)
./gradlew run --args='qa --mode rag --top-k 5 Что такое проект Соната-2041?'

# С RAG и реранкингом (более точная оценка релевантности через LLM)
./gradlew run --args='qa --mode rag --enable-reranking Что такое проект Соната-2041?'

# С RAG, реранкингом и фильтрацией по порогу релевантности
./gradlew run --args='qa --mode rag --enable-reranking --relevance-threshold 0.4 Что такое проект Соната-2041?'
```

**Особенности:**
- В режиме `rag` после ответа автоматически выводятся **источники данных** (документы, использованные для формирования ответа)
- Все вопросы и ответы сохраняются в базе данных для последующего просмотра через команду `history`

**Примечание:** При использовании `./gradlew run --args` слова вопроса передаются как отдельные аргументы, поэтому кавычки не нужны. Если вопрос содержит специальные символы, используйте одинарные кавычки снаружи: `./gradlew run --args='qa --mode rag Ваш вопрос здесь'`

Параметры:
- `--mode`: `plain` (без RAG, по умолчанию) или `rag` (с поиском по индексированным документам)
- `--top-k`: количество чанков для режима `rag` (по умолчанию: 5)
- `--enable-reranking`: включить реранкинг результатов через LLM для более точной оценки релевантности (медленнее, но точнее)
- `--relevance-threshold`: порог релевантности для фильтрации результатов (0.0-1.0). Результаты с оценкой ниже порога будут исключены
- `--rerank-top-k`: количество результатов для реранкинга (по умолчанию: `top-k * 2`). Используется только при включенном реранкинге
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)
- `--db-path`: путь к файлу SQLite (по умолчанию: ./index.db)

#### Реранкинг и фильтрация

При включенном реранкинге (`--enable-reranking`) система выполняет двухэтапную обработку результатов:

1. **Семантический поиск**: Получает больше результатов (по умолчанию `top-k * 2`, настраивается через `--rerank-top-k`)
2. **Предварительная фильтрация**: Отфильтровывает заведомо нерелевантные результаты перед реранкингом
3. **Реранкинг**: LLM переоценивает релевантность каждого чанка к запросу (оценка 0.0-1.0)
4. **Фильтрация по порогу**: Если задан `--relevance-threshold`, результаты с оценкой ниже порога исключаются
5. **Выбор топ-K**: Берется указанное количество лучших результатов (`--top-k`)

Статистика обработки выводится в консоль, показывая количество результатов на каждом этапе.

#### Вывод источников

В режиме `rag` после ответа автоматически выводятся источники данных:

```
Ответ:
[Текст ответа]

Источники:
  1. 01_Обзор_проекта_Соната_2041.txt (text/plain)
     Путь: /path/to/data/01_Обзор_проекта_Соната_2041.txt
  2. 03_Протокол_совещания_Соната_2041-05-21.txt (text/plain)
     Путь: /path/to/data/03_Протокол_совещания_Соната_2041-05-21.txt
```

### Просмотр истории диалогов

Команда `history` позволяет просматривать сохраненную историю вопросов и ответов:

```bash
# Список последних бесед
./gradlew run --args="history"

# Список последних 20 бесед
./gradlew run --args="history --limit 20"

# Просмотр конкретной беседы по ID
./gradlew run --args="history --conversation-id 1"

# Полный формат с детальной информацией об источниках
./gradlew run --args="history --conversation-id 1 --format full"
```

Параметры:
- `--conversation-id <ID>`: Показать сообщения конкретной беседы (без этого параметра показывается список бесед)
- `--limit <N>`: Количество бесед или сообщений для показа (по умолчанию: 10)
- `--format <short|full>`: Формат вывода (по умолчанию: short)
  - `short`: Краткая информация
  - `full`: Полная информация, включая пути к источникам и индексы чанков
- `--db-path`: Путь к файлу SQLite (по умолчанию: ./index.db)

**Формат вывода:**

При просмотре списка бесед:
```
Беседа #1
  Создана: 2024-01-15T10:30:00Z
  Обновлена: 2024-01-15T10:35:00Z
  Сообщений: 3 вопросов, 3 ответов
```

При просмотре конкретной беседы:
```
👤 Вопрос (rag):
Что такое проект Соната-2041?

🤖 Ответ (rag):
[Текст ответа]

📚 Источники:
  1. 01_Обзор_проекта_Соната_2041.txt (text/plain)
     Путь: /path/to/file.txt
```

**Особенности:**
- Все вопросы и ответы автоматически сохраняются при использовании команды `qa`
- Каждая беседа имеет уникальный ID и временные метки
- Источники сохраняются только для ответов в режиме `rag`
- История хранится в той же базе данных, что и индекс документов

### Ассистент разработчика (/help)

Команда `help` предоставляет интеллектуальную помощь по проекту, используя RAG для поиска в документации и безопасный MCP-доступ к репозиторию для работы с кодом.

```bash
# Показать возможности ассистента и примеры использования
./gradlew run --args="help"

# Вопрос по документации проекта (RAG)
./gradlew run --args='help "как запустить проект?"'

# Вопрос про переменные окружения (RAG)
./gradlew run --args='help "какие переменные окружения нужны?"'

# Поиск в коде (MCP)
./gradlew run --args='help "где в коде реализован эндпоинт /login?"'

# Статус изменений в репозитории (MCP)
./gradlew run --args='help "что изменено в текущей ветке?"'
```

**Параметры:**
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)

**Особенности:**

1. **Автоматическая индексация проекта**: При первом запуске команды автоматически строится индекс документации проекта (если его еще нет). Индекс кэшируется в `./project-index.db`.

2. **LLM-based роутинг инструментов**: LLM самостоятельно анализирует вопрос и решает, какие инструменты использовать:
   - **RAG** — поиск по документации проекта (README, docs, API specs, schemas, style guides)
   - **GIT_INFO** — информация о текущей ветке и статусе изменений
   - **CODE_SEARCH** — поиск по исходному коду (функции, классы, эндпоинты)
   - Может использовать несколько инструментов одновременно для комплексных вопросов

3. **Безопасный MCP-доступ к репозиторию**:
   - Получение текущей ветки (`git branch`)
   - Статус изменений (`git status`)
   - Поиск по коду через `ripgrep` (rg)
   - Чтение файлов с контекстом вокруг найденных строк
   - Все операции ограничены корнем проекта (безопасность)

4. **Формат ответа**:
   - Краткий ответ (3-10 предложений)
   - Блок "Источники" с путями к файлам
   - Блок "Следующие шаги" с рекомендациями

5. **Честность**: Если релевантная информация не найдена, ассистент честно сообщает об этом и предлагает альтернативные источники.

**Как работает LLM-роутинг:**

1. **Анализ вопроса**: LLM анализирует вопрос и определяет, какие инструменты нужны для ответа
2. **Выбор инструментов**: LLM может выбрать один или несколько инструментов:
   - RAG (документация)
   - GIT_INFO (ветка, статус)
   - CODE_SEARCH (поиск по коду с ключевым словом)
3. **Сбор контекста**: Выполняются выбранные операции, собирается контекст
4. **Генерация ответа**: LLM формирует финальный ответ на основе собранного контекста
5. **Вывод**: Ответ + источники + рекомендации следующих шагов

Это позволяет ассистенту гибко комбинировать разные источники информации для более полного и точного ответа.

### Командный ассистент (/team) - НОВОЕ!

Команда `team` предоставляет интегрированного ассистента для управления командой и проектом. Объединяет управление задачами, RAG по документации и MCP-доступ к репозиторию для комплексной поддержки команды разработчиков.

```bash
# Показать возможности командного ассистента
./gradlew run --args="team"

# Управление задачами
./gradlew run --args='team "покажи задачи с приоритетом high"'
./gradlew run --args='team "покажи все задачи в работе"'
./gradlew run --args='team "есть ли блокирующие задачи?"'

# Рекомендации и планирование
./gradlew run --args='team "что делать в первую очередь?"'
./gradlew run --args='team "какой статус у проекта?"'

# Создание задач
./gradlew run --args='team "создай задачу: добавить логирование в модуль авторизации"'

# Комплексные вопросы (задачи + документация + код)
./gradlew run --args='team "покажи критичные задачи и предложи что делать"'
./gradlew run --args='team "есть ли блокирующие задачи и как их решить?"'
./gradlew run --args='team "где реализована аутентификация и какие задачи с ней связаны?"'
```

**Параметры:**
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)

**Особенности:**

1. **Управление задачами через JSON**: Задачи хранятся в файле `tasks.json` в корне проекта. При первом запуске создается автоматически с примерами задач.

2. **LLM-based роутинг инструментов**: LLM анализирует вопрос и выбирает нужные инструменты:
   - **TASK_MANAGEMENT** — управление задачами (просмотр, создание, фильтрация, статистика)
   - **RAG** — поиск по документации проекта
   - **GIT_INFO** — информация о ветке и статусе репозитория
   - **CODE_SEARCH** — поиск по исходному коду

3. **Действия с задачами:**
   - **LIST** — просмотр задач с фильтрацией по приоритету/статусу
   - **CREATE** — создание новой задачи
   - **STATS** — статистика по задачам
   - **RECOMMEND** — рекомендации по приоритизации

4. **Приоритеты задач:**
   - CRITICAL — критичные (требуют немедленного решения)
   - HIGH — высокий приоритет
   - MEDIUM — средний приоритет
   - LOW — низкий приоритет

5. **Статусы задач:**
   - TODO — не начата
   - IN_PROGRESS — в работе
   - BLOCKED — заблокирована
   - DONE — завершена

6. **Интеграция с RAG и MCP**: Для комплексных вопросов ассистент может одновременно использовать несколько источников:
   - Текущие задачи из task manager
   - Документацию проекта через RAG
   - Информацию о репозитории через MCP
   - Поиск по коду для технических деталей

7. **Умные рекомендации**: LLM анализирует распределение задач по приоритетам и статусам, выявляет критичные и заблокированные задачи, предлагает оптимальную последовательность выполнения.

**Примеры работы:**

1. **Просмотр задач с фильтром:**
   ```
   Q: "покажи задачи с приоритетом high"
   → Показывает все задачи с приоритетом HIGH
   → Выводит детали: название, описание, статус, исполнитель, теги, сроки
   ```

2. **Рекомендации по приоритизации:**
   ```
   Q: "что делать в первую очередь?"
   → Анализирует все задачи
   → Выявляет критичные и заблокированные
   → Предлагает оптимальную последовательность
   → Даёт конкретные шаги для команды
   ```

3. **Комплексный анализ:**
   ```
   Q: "покажи критичные задачи и предложи решение"
   → TASK_MANAGEMENT: фильтрует критичные задачи
   → RAG: ищет информацию в документации
   → CODE_SEARCH: ищет релевантный код
   → Формирует комплексный ответ с рекомендациями
   ```

4. **Создание задачи:**
   ```
   Q: "создай задачу: добавить логирование в модуль авторизации"
   → LLM извлекает: название, описание, приоритет
   → Создаёт задачу в tasks.json
   → Показывает созданную задачу с ID
   ```

**Формат ответа:**
- Краткий анализ ситуации (3-10 предложений)
- Конкретные рекомендации и следующие шаги
- Блок "Источники" с файлами данных
- Блок "Следующие шаги" с практическими советами

**Примеры работы LLM-роутинга:**

1. **Вопрос о документации** → использует только RAG:
   ```
   Q: "Как запустить проект?"
   → LLM выбирает: RAG: yes, GIT_INFO: no, CODE_SEARCH: no
   → Ищет в README.md, docs/
   ```

2. **Вопрос о коде** → использует CODE_SEARCH:
   ```
   Q: "Где реализован эндпоинт /login?"
   → LLM выбирает: RAG: no, GIT_INFO: no, CODE_SEARCH: yes, KEYWORD: login
   → Ищет по коду через ripgrep
   → Показывает найденные файлы с контекстом
   ```

3. **Вопрос о состоянии репо** → использует GIT_INFO:
   ```
   Q: "Что изменено в текущей ветке?"
   → LLM выбирает: RAG: no, GIT_INFO: yes, CODE_SEARCH: no
   → Выполняет git branch и git status
   ```

4. **Комплексный вопрос** → использует несколько инструментов:
   ```
   Q: "Какие переменные окружения нужны и где они используются?"
   → LLM выбирает: RAG: yes, GIT_INFO: no, CODE_SEARCH: yes, KEYWORD: env
   → Ищет в документации (README, .env.example)
   → Ищет использование в коде
   → Комбинирует результаты для полного ответа
   ```

**Конфигурация проекта:**

Путь к проекту для индексации задаётся в `AssistantConfig.projectPath` (по умолчанию: `/home/vas/Documents/Projects/tg-keywords-monitoring`).

**Индексируемые источники:**

- README.md и все .md файлы в корне проекта
- Все файлы в директории `docs/**`
- CONTRIBUTING.md, STYLEGUIDE.md (если есть)
- openapi.yaml|yml, swagger.json
- Файлы схем: schema.sql, prisma.schema, migrations/** (если есть)
- Конфиги стиля/линтера: .editorconfig, .eslintrc*, .prettierrc*, pyproject.toml, ruff.toml и т.п.

### Персонализированный агент (/agent) - НОВОЕ!

Команда `agent` предоставляет интерактивный чат с персонализированным агентом, который адаптируется под ваши предпочтения, цели и ограничения на основе профиля пользователя.

```bash
# Интерактивный режим с персонализированным агентом
./gradlew run --args='agent'

# С начальным сообщением
./gradlew run --args='agent Привет! Помоги мне с дипломом'

# С указанием пути к профилю
./gradlew run --args='agent --profile ./personal/user_profile.json'
```

**Параметры:**
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)
- `--db-path`: Путь к файлу SQLite для сохранения истории (по умолчанию: ./index.db)
- `--profile`: Путь к файлу профиля пользователя (по умолчанию: ./personal/user_profile.json)

**Особенности:**

1. **Персонализация на основе профиля**: Агент использует информацию из файла `personal/user_profile.json` для адаптации под ваши предпочтения:
   - Имя и язык общения
   - Предпочтения по тону и стилю общения
   - Ограничения по времени (сколько часов в день доступно)
   - Цели и приоритеты
   - Правила поведения агента

2. **Интерактивный режим**: После запуска команды открывается интерактивная сессия с персонализированным агентом.

3. **История диалога**: Все сообщения автоматически сохраняются в базе данных и могут быть просмотрены через команду `history`.

4. **Специальные команды**:
   - `/exit`, `/quit` — выход из чата
   - `/clear` — начать новую беседу (создать новую conversation)
   - `/history` — показать историю текущей беседы
   - `/profile` — показать текущий профиль пользователя

5. **Умная персонализация**: Системный промпт формируется автоматически на основе всех параметров профиля, включая:
   - Как обращаться к пользователю (на "ты" или "вы")
   - Тон общения (деловой, дружелюбный и т.д.)
   - Вербальность (кратко, подробно)
   - Предпочтительные форматы ответов (списки, пошаговые инструкции)
   - Что избегать (вода, длинные рассуждения)
   - Ограничения по времени
   - Текущие цели и приоритеты
   - Правила поведения агента

**Формат профиля пользователя:**

Файл `personal/user_profile.json` должен содержать JSON со следующей структурой:

```json
{
  "user": {
    "name": "Василий",
    "timezone": "Europe/Moscow",
    "language": "ru"
  },
  "preferences": {
    "communication": {
      "addressing": "на вы",
      "tone": "деловой и доброжелательный",
      "verbosity": "коротко",
      "format_preferences": [
        "списки",
        "пошаговые инструкции",
        "короткие ответы с выводом"
      ],
      "avoid": [
        "вода",
        "слишком длинные ответы",
        "абстрактные советы без конкретики"
      ]
    },
    "learning": {
      "style": ["практика", "примеры", "быстрые понятные объяснения"],
      "pace": "средний"
    }
  },
  "constraints": {
    "time_per_day_minutes": 180,
    "days_per_week": 6
  },
  "goals": {
    "main": [
      "написать диплом",
      "сдать на права",
      "найти работу"
    ]
  },
  "agent_behavior": {
    "should_do": [
      "давать ответ максимально кратко",
      "если задача большая — разбивать на шаги",
      "в конце писать следующий шаг",
      "учитывать, что у пользователя есть 3 часа в день"
    ],
    "should_not_do": [
      "перегружать теорией",
      "выдавать длинные рассуждения",
      "придумывать факты о пользователе"
    ]
  },
  "priorities": {
    "current_focus": "диплом",
    "secondary": ["сдать на права", "поиск работы"]
  }
}
```

**Примеры использования:**

```bash
# Пример интерактивной сессии
$ ./gradlew run --args='agent'

╔══════════════════════════════════════════════════════════════╗
║         Персонализированный агент - Интерактивный режим      ║
╚══════════════════════════════════════════════════════════════╝

👤 Пользователь: Василий
🌐 Язык: ru
🎯 Текущий фокус: диплом

Доступные команды:
  • Любой вопрос - общайтесь с персонализированным агентом
  • '/exit', '/quit' - выход из чата
  • '/clear' - начать новую беседу
  • '/history' - показать историю текущей беседы
  • '/profile' - показать текущий профиль

👤 Вы: Помоги мне с дипломом

🤖 Ассистент: Конечно! Учитывая, что у вас есть 3 часа в день, предлагаю разбить работу на шаги:
1. Определить структуру диплома
2. Написать введение
3. Разработать основную часть
Следующий шаг: начните с определения структуры — это займет около 30 минут.

----------------------------------------------------------------

👤 Вы: /profile

════════════════════════════════════════════════════════════════
Профиль пользователя
════════════════════════════════════════════════════════════════

👤 Имя: Василий
🕐 Часовой пояс: Europe/Moscow
🌐 Язык: ru

💬 Предпочтения по общению:
  Обращение: на вы
  Тон: деловой и доброжелательный
  Вербальность: коротко

⏱️  Ограничения:
  Время в день: 3ч 0мин
  Дней в неделю: 6

🎯 Цели:
  • написать диплом
  • сдать на права
  • найти работу

📌 Текущий фокус: диплом
📌 Вторичные приоритеты: сдать на права, поиск работы

----------------------------------------------------------------

👤 Вы: /exit

Завершение чата. До встречи!
```

**Отличия от команды `chat`:**

- **Персонализация**: Агент адаптируется под ваш профиль, учитывая предпочтения, ограничения и цели
- **Контекст целей**: Агент знает ваши текущие цели и приоритеты, может предлагать релевантные советы
- **Учет ограничений**: Агент учитывает ваше доступное время и темп работы
- **Следование правилам**: Агент следует правилам поведения из профиля (что должен и не должен делать)

### Интерактивный чат с LLM (/chat) - НОВОЕ!

Команда `chat` предоставляет интерактивный режим общения с LLM с поддержкой специальных команд, включая автоматический релиз приложения.

**⚠️ Важно:** Для интерактивного режима используйте скрипт `./chat.sh` вместо `./gradlew run`

```bash
# Интерактивный чат (РЕКОМЕНДУЕТСЯ)
./chat.sh

# Или с начальным сообщением через Gradle
./gradlew run --args='chat Привет! Расскажи мне о погоде'
```

**Параметры:**
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)
- `--db-path`: Путь к файлу SQLite для сохранения истории (по умолчанию: ./index.db)

**Особенности:**

1. **Интерактивный режим**: После запуска команды открывается интерактивная сессия, где можно задавать вопросы и получать ответы от LLM в реальном времени.

2. **История диалога**: Все сообщения автоматически сохраняются в базе данных и могут быть просмотрены через команду `history`.

3. **Специальные команды**:
   - `/exit`, `/quit` — выход из чата
   - `/clear` — начать новую беседу (создать новую conversation)
   - `/history` — показать историю текущей беседы

4. **Автоматический релиз приложения**: При обнаружении намерения релиза в сообщении (определяется через LLM) автоматически запускается процесс релиза через MCP.

5. **Умное определение намерений**: LLM анализирует каждое сообщение и определяет, является ли это запросом на выполнение релиза, вместо простой проверки по ключевым словам.

**Примеры использования:**

```bash
# Пример интерактивной сессии
$ ./gradlew run --args='chat'

╔══════════════════════════════════════════════════════════════╗
║              Чат с LLM - Интерактивный режим                 ║
╚══════════════════════════════════════════════════════════════╝

Доступные команды:
  • Любой вопрос - общайтесь с LLM
  • 'зарелизь приложение', 'сделай релиз' - запуск релиза
  • '/exit', '/quit' - выход из чата
  • '/clear' - начать новую беседу
  • '/history' - показать историю текущей беседы

👤 Вы: Привет! Как дела?

🤖 Ассистент: Привет! У меня всё отлично, спасибо! Я готов помочь тебе...

----------------------------------------------------------------

👤 Вы: Расскажи мне о Kotlin корутинах

🤖 Ассистент: Kotlin корутины - это легковесные потоки выполнения...

----------------------------------------------------------------

👤 Вы: зарелизь приложение

🚀 Обнаружен запрос на релиз приложения...

   1. Проверка локальной директории... ✅
   2. Проверка SSH соединения с my_mon_bot... ✅
   3. Получение списка файлов... найдено 15 файлов
   
   Файлы для загрузки:
      • main.py
      • config.py
      • ...
   
   4. Загрузка файлов на сервер... 

✅ Релиз успешно завершен!

📊 Статистика:
   • Загружено файлов: 15
   • Время выполнения: 3.2 секунд
   • Удаленная директория: /root/release на my_mon_bot

----------------------------------------------------------------

👤 Вы: /exit

Завершение чата. До встречи!
```

### MCP для релиза приложения - НОВОЕ!

Новый MCP-клиент `ReleaseMcp` предоставляет функциональность для автоматического релиза приложения на удаленный сервер через SSH.

**Конфигурация:**
- SSH конфигурация: `my_mon_bot` (настроена в `~/.ssh/config`)
- Локальная директория: `/home/vas/Documents/Projects/EchoBot`
- Удаленная директория: `/root/release`

**Функции MCP:**

1. **testConnection()** — проверка доступности SSH соединения
2. **checkLocalDirectory()** — проверка существования локальной директории
3. **getLocalFiles()** — получение списка файлов для загрузки
4. **createRemoteDirectory()** — создание удаленной директории
5. **release()** — выполнение полного релиза (синхронизация файлов через rsync)
6. **getRemoteInfo()** — получение информации о файлах на сервере
7. **cleanRemoteDirectory()** — очистка удаленной директории

**Процесс релиза:**

1. Проверка локальной директории с файлами
2. Проверка SSH соединения с сервером
3. Получение списка файлов для загрузки
4. Создание удаленной директории (если не существует)
5. Синхронизация файлов через rsync
6. Проверка результата и получение информации о загруженных файлах

**Интеграция с chat:**

При использовании команды `chat`, если в сообщении обнаружены ключевые слова релиза:
- "зарелизь", "зарелизить"
- "сделай релиз", "выполни релиз", "запусти релиз"
- "нужно зарелизить", "нужен релиз"
- "загрузи на сервер"
- "deploy", "деплой"

Автоматически запускается процесс релиза с выводом подробной статистики:
- Проверка предварительных условий
- Прогресс загрузки файлов
- Статистика: количество файлов, время выполнения
- Список загруженных файлов
- Содержимое удаленной директории

**Требования:**

1. Настроенный SSH-ключ для беспарольного входа:
   ```bash
   # ~/.ssh/config
   Host my_mon_bot
       HostName your-server.com
       User root
       IdentityFile ~/.ssh/id_rsa
   ```

2. Установленный `rsync` на локальной машине и сервере:
   ```bash
   sudo apt-get install rsync  # для Ubuntu/Debian
   ```

3. Доступные права для записи в `/root/release` на сервере.

**Безопасность:**

- Все операции выполняются через настроенный SSH-конфиг
- Используется rsync с флагом `--delete` (удаляет файлы на сервере, которых нет локально)
- Проверка путей для предотвращения выхода за пределы разрешенных директорий
- Логирование всех операций релиза в истории чата


## Примеры

```bash
# Индексация текущей директории
./gradlew run --args="index . --chunk-size 512 --overlap 50"

# Индексация с дополнительными исключениями
./gradlew run --args='index . --exclude "*.test.ts" --exclude "coverage/**"'

# Поиск
./gradlew run --args="search \"как работает корутины\" --top-k 5"

# Статистика
./gradlew run --args="stats"

# Вопрос с RAG без реранкинга
./gradlew run --args='qa --mode rag Что такое проект Соната-2041?'

# Вопрос с RAG и реранкингом
./gradlew run --args='qa --mode rag --enable-reranking Что такое проект Соната-2041?'

# Вопрос с RAG, реранкингом и фильтрацией
./gradlew run --args='qa --mode rag --enable-reranking --relevance-threshold 0.4 --top-k 3 Что такое проект Соната-2041?'

# Просмотр истории диалогов
./gradlew run --args="history"

# Просмотр конкретной беседы
./gradlew run --args="history --conversation-id 1 --format full"

# Ассистент разработчика - справка
./gradlew run --args="help"

# Ассистент разработчика - вопрос по документации
./gradlew run --args='help "как запустить проект?"'

# Ассистент разработчика - поиск в коде
./gradlew run --args='help "где реализован эндпоинт /login?"'

# Ассистент разработчика - статус изменений
./gradlew run --args='help "что изменено в текущей ветке?"'

# Командный ассистент - просмотр задач
./gradlew run --args='team "покажи задачи с приоритетом high"'

# Командный ассистент - рекомендации
./gradlew run --args='team "что делать в первую очередь?"'

# Командный ассистент - блокирующие задачи
./gradlew run --args='team "есть ли блокирующие задачи?"'

# Командный ассистент - создание задачи
./gradlew run --args='team "создай задачу: добавить мониторинг производительности"'

# Интерактивный чат с LLM
./gradlew run --args='chat'

# Интерактивный чат с начальным сообщением
./gradlew run --args='chat Привет! Расскажи о себе'

# Персонализированный агент
./gradlew run --args='agent'

# Персонализированный агент с начальным сообщением
./gradlew run --args='agent Помоги мне с дипломом'
```

## Поддерживаемые форматы файлов

- Markdown: `.md`
- Текст: `.txt`
- Код: `.kt`, `.java`, `.js`, `.ts`, `.jsx`, `.tsx`, `.py`, `.rs`, `.go`, `.cpp`, `.c`, `.h`, `.hpp`, `.cs`, `.rb`, `.php`, `.swift`, `.scala`, `.clj`
- Конфигурация: `.yaml`, `.yml`, `.json`, `.xml`, `.html`, `.css`, `.sh`, `.sql`, `.toml`, `.ini`, `.conf`
- Специальные файлы: `.editorconfig`, `.eslintrc*`, `.prettierrc*`, `pyproject.toml`, `ruff.toml`, `openapi.yaml|yml`, `swagger.json`, `prisma.schema`, `schema.sql`

Максимальный размер файла: 10MB

## Архитектура

- **Инкрементальная индексация**: Файлы переиндексируются только при изменении (проверка по хешу SHA-256)
- **Чанкинг с overlap**: Текст разбивается на чанки с перекрытием для сохранения контекста
- **Семантический поиск**: Использует косинусное сходство векторов эмбеддингов
- **Реранкинг**: Опциональная переоценка релевантности результатов через LLM для повышения точности
- **Фильтрация по порогу**: Исключение нерелевантных результатов на основе оценки релевантности
- **SQLite**: Локальное хранение индекса с поддержкой каскадного удаления
- **Ассистент разработчика**: Интеграция RAG и безопасного MCP-доступа к репозиторию для помощи в разработке
- **Безопасный MCP-клиент**: Ограниченный доступ к git-командам и файловой системе проекта с проверкой путей
- **Task Manager MCP**: Управление задачами команды через JSON-файл с поддержкой приоритетов, статусов, назначений и тегов
- **Командный ассистент**: Интеграция управления задачами, RAG и MCP для комплексной поддержки команды разработчиков
- **LLM-based роутинг**: LLM самостоятельно анализирует вопрос и выбирает нужные инструменты (TASK_MANAGEMENT, RAG, git info, code search) или их комбинацию
- **Многоступенчатая генерация**: Сначала LLM выбирает инструменты, затем собирается контекст, затем формируется финальный ответ
- **Умная приоритизация**: Анализ задач по критичности, выявление блокеров, рекомендации по оптимальной последовательности выполнения
- **Гибкая система исключений**: Поддержка паттернов исключения (glob), автоматическое использование .gitignore, дефолтные исключения для типичных артефактов сборки и зависимостей
- **Интерактивный чат**: Команда `chat` для непрерывного общения с LLM с сохранением истории и поддержкой специальных команд
- **Персонализированный агент**: Команда `agent` для общения с агентом, адаптированным под профиль пользователя (предпочтения, цели, ограничения)
- **Профиль пользователя**: JSON-конфигурация для персонализации поведения агента (тон, вербальность, цели, ограничения по времени)
- **Release MCP**: Автоматизация релиза приложения через SSH с использованием rsync, интегрирован в интерактивный чат
- **Умное определение намерений**: Автоматическое распознавание команд релиза в естественном языке и их выполнение через соответствующий MCP

## Структура базы данных

### Таблицы индексации

- `documents`: Метаданные файлов
- `chunks`: Текстовые чанки с позициями
- `embeddings`: Векторы эмбеддингов

### Таблицы истории диалогов

- `conversations`: Беседы (сессии диалога)
  - `id`: Уникальный идентификатор беседы
  - `created_at`: Время создания беседы
  - `updated_at`: Время последнего обновления беседы

- `messages`: Сообщения в беседах
  - `id`: Уникальный идентификатор сообщения
  - `conversation_id`: Ссылка на беседу
  - `role`: Роль отправителя (`user` или `assistant`)
  - `content`: Текст сообщения
  - `mode`: Режим запроса (`plain` или `rag`)
  - `sources_json`: JSON с информацией об источниках (только для RAG режима)
  - `created_at`: Время создания сообщения

**Примечание:** Все таблицы создаются автоматически при первом запуске приложения.

