# Quick Start Guide

## Быстрый старт

### 1. Запуск приложения

```bash
./gradlew run
```

При первом запуске приложение автоматически создаст distribution для weather server (~5 секунд).
Последующие запуски будут быстрее.

### 2. Проверка работы

Вы должны увидеть:
```
Preparing weather server distribution...
Starting weather server: /path/to/weather-server/bin/weather-server
[Weather Server] Weather MCP Server started
[MCP Client] Sending: {"id":1,"method":"initialize"...}
[MCP Client] Received: {"id":1,"result":...}
MCP server initialized successfully
✓ MCP Weather Server запущен. Доступные инструменты: [get_current_weather]
```

### 3. Примеры запросов

```
Пользователь: Какая сейчас погода в Москве?
/end
```

Вы увидите:
```
🔧 ИИ вызывает инструменты:
  - get_current_weather с аргументами: {"city":"Moscow"}
  ✓ Результат: {...}

ИИ: [ответ с информацией о погоде]
```

## Команды

- `/exit` - выйти (показывает финальную статистику)
- `/help` - справка
- `/clear` - очистить историю
- `/cleardb` - очистить базу данных
- `/stats` - показать статистику токенов
- `/end` - завершить ввод многострочного сообщения

## Устранение проблем

### Ошибка парсинга tool_calls?

**Симптом:**
```
Ошибка парсинга строки NDJSON: Expected beginning of the string, but got {
```

**Решение:** ✅ Исправлено! Создан кастомный сериализатор `ArgumentsSerializer`, который поддерживает оба формата `arguments` (строка и объект). См. [FIX_TOOL_CALLS_PARSING.md](FIX_TOOL_CALLS_PARSING.md)

### Сервер не запускается?

```bash
# Создайте distribution вручную
./gradlew :weather-server:installDist

# Проверьте создание
ls -la weather-server/build/install/weather-server/bin/

# Протестируйте сервер
echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' | \
  weather-server/build/install/weather-server/bin/weather-server
```

Должно вернуть JSON ответ с `"result"`.

### AI не использует инструменты?

- Убедитесь, что сервер запустился (см. логи выше)
- Используйте явные запросы: "Узнай погоду в Москве" или "Какая температура в Лондоне?"
- Проверьте, что модель поддерживает tool calling (llama3.1:8b поддерживает)

### Тестирование без запуска приложения

```bash
./test-weather-server.sh
```

Это протестирует сервер изолированно.

## Примеры запросов о погоде

✅ **Хорошие запросы:**
- "Какая сейчас погода в Москве?"
- "Расскажи о погоде в Лондоне"
- "Какая температура в Нью-Йорке?"
- "Стоит ли брать зонт в Париже?"

❌ **Плохие запросы:**
- "Какая будет погода завтра?" (прогноз не поддерживается)
- "Погода" (нужно указать город)
- Названия на русском могут не работать - используйте транслитерацию: "Moscow", "London"

## Логи

### Логи MCP клиента:
```
[MCP Client] Sending: ...
[MCP Client] Received: ...
```

### Логи MCP сервера:
```
[Weather Server] Weather MCP Server started
[Weather Server] Received: ...
[Weather Server] Sent: ...
```

Если видите ошибки - смотрите раздел Troubleshooting в WEATHER_MCP_README.md

## Требования

- ✅ Java 21+
- ✅ Ollama с моделью llama3.1:8b или выше
- ✅ Интернет соединение (для WeatherAPI)
- ✅ Gradle (встроен в проект)

## Логирование

Приложение автоматически логирует все операции:

### Что видно в консоли (stderr):
```
[Ollama API] REQUEST [2025-12-17 00:45:23]
  → POST http://localhost:11434/api/chat
  → С инструментами (tool calling enabled)

[Tool Call] [2025-12-17 00:45:24]
  🔧 Инструмент: get_current_weather
  ✓ Результат: {...}
```

### Файл логов:

**ollama_api.log** - логи Ollama:
- Все запросы и ответы
- Tool calls
- Ошибки
- Статистика

**weather_api.log** - логи Weather API:
- Запросы к weatherapi.com
- Ответы с погодой
- Ошибки API

### Статистика:
```
# В процессе работы
/stats

# При выходе - автоматически
/exit
```

## Дополнительная информация

- [LOGGING.md](LOGGING.md) - документация по логированию
- [WEATHER_MCP_README.md](WEATHER_MCP_README.md) - полная документация MCP интеграции
- [IMPLEMENTATION_SUMMARY.md](IMPLEMENTATION_SUMMARY.md) - техническое описание
- [README.md](README.md) - общее описание приложения

