# План решения: Добавление MCP модуля для работы с Android устройствами через ADB

## Цель
Создать новый Gradle модуль `adb-server` по аналогии с `weather-server`, который будет предоставлять MCP инструмент для получения списка подключенных Android устройств/эмуляторов через команду `adb devices`.

## Структура решения

### Этап 1: Создание структуры нового Gradle модуля

#### 1.1. Обновление `settings.gradle.kts`
- Добавить новый модуль `adb-server` в список include проектов
- Файл: `settings.gradle.kts`
- Изменение: добавить строку `include("adb-server")`

#### 1.2. Создание `adb-server/build.gradle.kts`
- Скопировать структуру из `weather-server/build.gradle.kts`
- Настроить зависимости:
  - `kotlin("jvm")` версия 2.2.20
  - `kotlin("plugin.serialization")` версия 2.2.20
  - `application` plugin
  - Зависимости: `kotlinx-serialization-json`, `kotlinx-coroutines-core`
- Установить `mainClass` на `org.example.adb.AdbServerKt`
- Настроить Java toolchain на версию 21

#### 1.3. Создание структуры директорий
- Создать директорию: `adb-server/src/main/kotlin/org/example/adb/`
- Файлы для создания:
  - `AdbServer.kt` - основной MCP сервер
  - `AdbClient.kt` - клиент для выполнения ADB команд
  - `AdbModels.kt` - модели данных для устройств

### Этап 2: Реализация ADB клиента

#### 2.1. Создание `AdbModels.kt`
- Определить data class `AndroidDevice`:
  - `serialNumber: String` - серийный номер устройства
  - `status: String` - статус (device, offline, unauthorized, etc.)
  - `type: String?` - тип устройства (эмулятор/физическое устройство)
- Определить data class `AdbDevicesResponse`:
  - `devices: List<AndroidDevice>` - список устройств
  - `totalCount: Int` - общее количество устройств
  - `onlineCount: Int` - количество онлайн устройств

#### 2.2. Создание `AdbClient.kt`
- Класс `AdbClient`:
  - Метод `getDevices(): Result<AdbDevicesResponse>`
    - Выполняет команду `adb devices -l` (или `adb devices`)
    - Парсит вывод команды
    - Определяет тип устройства (эмулятор по префиксу `emulator-` или по `model:` в выводе `adb devices -l`)
    - Возвращает структурированный список устройств
  - Метод `executeAdbCommand(command: String): Result<String>`
    - Выполняет произвольную ADB команду
    - Обрабатывает ошибки выполнения
    - Возвращает результат выполнения
  - Обработка ошибок:
    - ADB не найден в PATH
    - Устройства не подключены
    - Ошибки выполнения команды

### Этап 3: Реализация MCP сервера

#### 3.1. Создание `AdbServer.kt`
- Реализовать JSON-RPC сервер по аналогии с `WeatherServer.kt`:
  - Обработка метода `initialize`:
    - Возвращает информацию о сервере: `name: "adb-server"`, `version: "1.0.0"`
    - Указывает protocolVersion: `"2024-11-05"`
  - Обработка метода `tools/list`:
    - Возвращает список инструментов с одним инструментом:
      - `name: "list_android_devices"`
      - `description: "Получить список подключенных Android устройств и эмуляторов через ADB. Возвращает серийные номера, статусы и типы устройств."`
      - `inputSchema`: объект без обязательных параметров (или с опциональными фильтрами)
  - Обработка метода `tools/call`:
    - Для инструмента `list_android_devices`:
      - Вызывает `AdbClient.getDevices()`
      - Форматирует ответ в JSON:
        - Список устройств с деталями
        - Статистика (всего устройств, онлайн устройств)
        - Форматированное текстовое представление для LLM
      - Возвращает результат в формате MCP:
        ```json
        {
          "content": [{
            "type": "text",
            "text": "<форматированный ответ>"
          }]
        }
        ```

#### 3.2. Форматирование ответа
- Создать функцию `formatDevicesResponse()`:
  - Красивое форматирование списка устройств
  - Группировка по статусам (online, offline, unauthorized)
  - Отображение типа устройства (эмулятор/физическое)
  - Статистика в начале ответа

### Этап 4: Интеграция с основным приложением

#### 4.1. Обновление `McpClientManager.kt`
- Добавить метод `startAdbServer(): Boolean`:
  - По аналогии с `startWeatherServer()`
  - Собирает distribution через `gradlew :adb-server:installDist`
  - Запускает процесс `adb-server/build/install/adb-server/bin/adb-server`
  - Инициализирует соединение через `initialize`
  - Получает список инструментов через `tools/list`
  - Сохраняет список инструментов в `availableTools`
- Модифицировать класс для поддержки нескольких серверов:
  - Вариант 1: Хранить список процессов серверов
  - Вариант 2: Создать отдельные экземпляры для каждого сервера
  - **Рекомендуется Вариант 1**: хранить список процессов и объединять инструменты от всех серверов

#### 4.2. Обновление `Main.kt`
- После запуска `weather-server`, запустить `adb-server`:
  ```kotlin
  val adbStarted = mcpClientManager.startAdbServer()
  if (adbStarted) {
      val tools = mcpClientManager.getAvailableTools()
      println("✓ MCP ADB Server запущен. Доступные инструменты: ${tools.map { it.name }}")
  }
  ```
- При закрытии приложения закрыть оба сервера

### Этап 5: Обработка ошибок и edge cases

#### 5.1. Обработка отсутствия ADB
- Проверка наличия `adb` в PATH при запуске сервера
- Возврат понятного сообщения об ошибке, если ADB не найден
- Возможность работы сервера даже если ADB недоступен (возврат пустого списка)

#### 5.2. Парсинг вывода `adb devices`
- Обработка различных форматов вывода:
  - Стандартный формат: `serial_number    status`
  - Расширенный формат: `adb devices -l` с дополнительной информацией
- Обработка пустого списка устройств
- Обработка устройств в статусе `unauthorized`

#### 5.3. Логирование
- Создать `AdbApiLogger` по аналогии с `WeatherApiLogger`
- Логировать:
  - Выполняемые ADB команды
  - Результаты выполнения
  - Ошибки

### Этап 6: Тестирование

#### 6.1. Тестирование ADB клиента
- Тестирование с подключенными устройствами
- Тестирование без устройств
- Тестирование с эмуляторами
- Тестирование с устройствами в разных статусах

#### 6.2. Тестирование интеграции
- Проверка запуска сервера
- Проверка получения списка инструментов
- Проверка вызова инструмента через LLM
- Проверка форматирования ответа

## Детальная структура файлов

```
day15/
├── settings.gradle.kts                    # + include("adb-server")
├── adb-server/
│   ├── build.gradle.kts                   # новый файл
│   └── src/
│       └── main/
│           └── kotlin/
│               └── org/
│                   └── example/
│                       └── adb/
│                           ├── AdbServer.kt        # MCP сервер
│                           ├── AdbClient.kt        # ADB клиент
│                           └── AdbModels.kt        # Модели данных
└── src/
    └── main/
        └── kotlin/
            ├── McpClientManager.kt         # + startAdbServer()
            └── Main.kt                     # + запуск adb-server
```

## Последовательность реализации

1. **Создание структуры модуля** (Этап 1)
   - Обновить `settings.gradle.kts`
   - Создать `adb-server/build.gradle.kts`
   - Создать структуру директорий

2. **Реализация ADB клиента** (Этап 2)
   - Создать модели данных
   - Реализовать `AdbClient` с парсингом вывода `adb devices`

3. **Реализация MCP сервера** (Этап 3)
   - Создать `AdbServer.kt` с обработкой JSON-RPC запросов
   - Реализовать инструмент `list_android_devices`

4. **Интеграция** (Этап 4)
   - Обновить `McpClientManager` для поддержки нескольких серверов
   - Обновить `Main.kt` для запуска adb-server

5. **Обработка ошибок** (Этап 5)
   - Добавить проверки и логирование

6. **Тестирование** (Этап 6)
   - Протестировать все сценарии

## Пример использования после реализации

Пользователь в чате может написать:
```
Покажи список подключенных Android устройств
```

LLM автоматически вызовет инструмент `list_android_devices` и вернет результат:
```
📱 Android устройства (всего: 2, онлайн: 1)

✅ Онлайн устройства:
  • emulator-5554 (Эмулятор) - device
  • R58M1234567 (Физическое устройство) - device

⚠️ Офлайн устройства:
  • emulator-5556 (Эмулятор) - offline
```

## Зависимости и требования

- **ADB**: Должен быть установлен и доступен в PATH
- **Kotlin**: Версия 2.2.20
- **Java**: Версия 21
- **Gradle**: Текущая версия проекта

## Примечания

- Сервер должен работать независимо от weather-server
- Инструменты от обоих серверов должны быть доступны одновременно
- При отсутствии ADB сервер должен корректно обрабатывать ошибку
- Форматирование ответа должно быть понятным для LLM и пользователя

