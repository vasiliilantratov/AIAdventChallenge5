# Ассистент для поддержки пользователей проекта Соната-2041

Консольное приложение на Kotlin для автоматизированной поддержки пользователей с использованием:
- **RAG** (Retrieval-Augmented Generation) - ответы на основе документации и FAQ
- **MCP** (Model Context Protocol) - интеграция с CRM системой для персонализированных ответов
- **Семантический поиск** по локальным документам с использованием Ollama и SQLite

## 🎯 Основные возможности

- ✅ **RAG для поддержки пользователей:** Отвечает на вопросы используя документацию и FAQ
- ✅ **MCP интеграция:** Работает с данными пользователей и тикетами из JSON (имитация CRM)
- ✅ **Персонализация:** Учитывает контекст пользователя (активные тикеты, история обращений)
- ✅ **Семантический поиск:** Находит релевантную информацию по смыслу, а не по ключевым словам
- ✅ **Реранкинг:** Опциональная переоценка результатов через LLM для повышения точности
- ✅ **История диалогов:** Автоматическое сохранение всех вопросов и ответов

📚 **[Подробная инструкция по настройке и использованию](SETUP_AND_USAGE.md)**  
🎭 **[Демонстрационные примеры с объяснениями](DEMO_EXAMPLES.md)**

## Требования

- Java 21+
- Gradle
- Ollama с установленной моделью `nomic-embed-text:latest`

### Установка Ollama модели

```bash
ollama pull nomic-embed-text:latest
```

## Сборка

```bash
./gradlew build
```

## Использование

### Индексация директории

```bash
./gradlew run --args="index /path/to/documents --chunk-size 512 --overlap 50"
```

Параметры:
- `--chunk-size`: Размер чанка в символах (по умолчанию: 512)
- `--overlap`: Размер overlap между чанками (по умолчанию: 50)
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)
- `--db-path`: Путь к файлу SQLite (по умолчанию: ./index.db)

### Поиск

```bash
./gradlew run --args="search \"ваш запрос\" --top-k 10"
```

Параметры:
- `--top-k`: Количество результатов (по умолчанию: 10)
- `--ollama-url`: URL Ollama сервера
- `--db-path`: Путь к файлу SQLite

### Статистика индекса

```bash
./gradlew run --args="stats"
```

### Очистка индекса

```bash
./gradlew run --args="clear"
```

### Удаление документа из индекса

```bash
./gradlew run --args="remove /path/to/file.md"
```

### Вопрос к LLM (режимы с RAG / без RAG + MCP контекст)

Команда `qa` отправляет вопрос в LLM в одном из двух режимов. **Все вопросы и ответы автоматически сохраняются в истории диалога.**

```bash
# Без RAG (модель отвечает без учёта индекса)
./gradlew run --args='qa --mode plain Что такое проект Соната-2041?'

# С RAG (поиск релевантных чанков + ответ по контексту)
./gradlew run --args='qa --mode rag --top-k 5 Что такое проект Соната-2041?'

# С RAG и реранкингом (более точная оценка релевантности через LLM)
./gradlew run --args='qa --mode rag --enable-reranking Что такое проект Соната-2041?'

# С RAG, реранкингом и фильтрацией по порогу релевантности
./gradlew run --args='qa --mode rag --enable-reranking --relevance-threshold 0.4 Что такое проект Соната-2041?'

# 🆕 С контекстом пользователя из CRM (MCP)
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация?'

# 🆕 Полная конфигурация с MCP контекстом
./gradlew run --args='qa --mode rag --user-id user_002 --enable-reranking --relevance-threshold 0.5 Почему поиск не находит документы?'
```

**Особенности:**
- В режиме `rag` после ответа автоматически выводятся **источники данных** (документы, использованные для формирования ответа)
- Все вопросы и ответы сохраняются в базе данных для последующего просмотра через команду `history`
- 🆕 При указании `--user-id` система загружает контекст пользователя из CRM (активные тикеты, историю обращений)
- 🆕 LLM формирует персонализированный ответ с учетом контекста пользователя и найденной документации

**Примечание:** При использовании `./gradlew run --args` слова вопроса передаются как отдельные аргументы, поэтому кавычки не нужны. Если вопрос содержит специальные символы, используйте одинарные кавычки снаружи: `./gradlew run --args='qa --mode rag Ваш вопрос здесь'`

Параметры:
- `--mode`: `plain` (без RAG, по умолчанию) или `rag` (с поиском по индексированным документам)
- `--top-k`: количество чанков для режима `rag` (по умолчанию: 5)
- `--enable-reranking`: включить реранкинг результатов через LLM для более точной оценки релевантности (медленнее, но точнее)
- `--relevance-threshold`: порог релевантности для фильтрации результатов (0.0-1.0). Результаты с оценкой ниже порога будут исключены
- `--rerank-top-k`: количество результатов для реранкинга (по умолчанию: `top-k * 2`). Используется только при включенном реранкинге
- `--user-id`: 🆕 ID пользователя из CRM для загрузки контекста (тикеты, история)
- `--crm-data`: 🆕 путь к JSON файлу с данными CRM (по умолчанию: data/crm_data.json)
- `--ollama-url`: URL Ollama сервера (по умолчанию: http://localhost:11434)
- `--db-path`: путь к файлу SQLite (по умолчанию: ./index.db)

#### Реранкинг и фильтрация

При включенном реранкинге (`--enable-reranking`) система выполняет двухэтапную обработку результатов:

1. **Семантический поиск**: Получает больше результатов (по умолчанию `top-k * 2`, настраивается через `--rerank-top-k`)
2. **Предварительная фильтрация**: Отфильтровывает заведомо нерелевантные результаты перед реранкингом
3. **Реранкинг**: LLM переоценивает релевантность каждого чанка к запросу (оценка 0.0-1.0)
4. **Фильтрация по порогу**: Если задан `--relevance-threshold`, результаты с оценкой ниже порога исключаются
5. **Выбор топ-K**: Берется указанное количество лучших результатов (`--top-k`)

Статистика обработки выводится в консоль, показывая количество результатов на каждом этапе.

#### Вывод источников

В режиме `rag` после ответа автоматически выводятся источники данных:

```
Ответ:
[Текст ответа]

Источники:
  1. 01_Обзор_проекта_Соната_2041.txt (text/plain)
     Путь: /path/to/data/01_Обзор_проекта_Соната_2041.txt
  2. 03_Протокол_совещания_Соната_2041-05-21.txt (text/plain)
     Путь: /path/to/data/03_Протокол_совещания_Соната_2041-05-21.txt
```

### Просмотр истории диалогов

Команда `history` позволяет просматривать сохраненную историю вопросов и ответов:

```bash
# Список последних бесед
./gradlew run --args="history"

# Список последних 20 бесед
./gradlew run --args="history --limit 20"

# Просмотр конкретной беседы по ID
./gradlew run --args="history --conversation-id 1"

# Полный формат с детальной информацией об источниках
./gradlew run --args="history --conversation-id 1 --format full"
```

Параметры:
- `--conversation-id <ID>`: Показать сообщения конкретной беседы (без этого параметра показывается список бесед)
- `--limit <N>`: Количество бесед или сообщений для показа (по умолчанию: 10)
- `--format <short|full>`: Формат вывода (по умолчанию: short)
  - `short`: Краткая информация
  - `full`: Полная информация, включая пути к источникам и индексы чанков
- `--db-path`: Путь к файлу SQLite (по умолчанию: ./index.db)

**Формат вывода:**

При просмотре списка бесед:
```
Беседа #1
  Создана: 2024-01-15T10:30:00Z
  Обновлена: 2024-01-15T10:35:00Z
  Сообщений: 3 вопросов, 3 ответов
```

При просмотре конкретной беседы:
```
👤 Вопрос (rag):
Что такое проект Соната-2041?

🤖 Ответ (rag):
[Текст ответа]

📚 Источники:
  1. 01_Обзор_проекта_Соната_2041.txt (text/plain)
     Путь: /path/to/file.txt
```

**Особенности:**
- Все вопросы и ответы автоматически сохраняются при использовании команды `qa`
- Каждая беседа имеет уникальный ID и временные метки
- Источники сохраняются только для ответов в режиме `rag`
- История хранится в той же базе данных, что и индекс документов

## Примеры

### Базовое использование

```bash
# Индексация документов и FAQ
./gradlew run --args="index data --chunk-size 512 --overlap 50"

# Поиск по документам
./gradlew run --args="search \"авторизация в системе\" --top-k 5"

# Статистика
./gradlew run --args="stats"

# Вопрос с RAG
./gradlew run --args='qa --mode rag Что такое проект Соната-2041?'
```

### 🆕 Использование с MCP контекстом

```bash
# Пользователь с активным тикетом про авторизацию
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация?'

# Пользователь с решенными тикетами (система использует историю решений)
./gradlew run --args='qa --mode rag --user-id user_005 Не могу подключиться к Ollama'

# Критическая проблема инфраструктуры с полной конфигурацией
./gradlew run --args='qa --mode rag --user-id user_003 --enable-reranking --relevance-threshold 0.5 Что делать с перегревом сервера Лира-2?'

# Вопрос о конфигурации от специалиста
./gradlew run --args='qa --mode rag --user-id user_005 --enable-reranking Как настроить порог релевантности?'
```

### Продвинутое использование

```bash
# С реранкингом для повышения точности
./gradlew run --args='qa --mode rag --enable-reranking Как работает семантический поиск?'

# С фильтрацией по порогу релевантности
./gradlew run --args='qa --mode rag --enable-reranking --relevance-threshold 0.4 --top-k 3 Что такое кварцевый шёпот?'

# Просмотр истории диалогов
./gradlew run --args="history"

# Просмотр конкретной беседы
./gradlew run --args="history --conversation-id 1 --format full"
```

📚 **Больше примеров с объяснениями:** [DEMO_EXAMPLES.md](DEMO_EXAMPLES.md)

## Поддерживаемые форматы файлов

- Markdown: `.md`
- Текст: `.txt`
- Код: `.kt`, `.java`, `.js`, `.ts`, `.jsx`, `.tsx`, `.py`, `.rs`, `.go`, `.cpp`, `.c`, `.h`, `.hpp`, `.cs`, `.rb`, `.php`, `.swift`, `.scala`, `.clj`
- Конфигурация: `.yaml`, `.yml`, `.json`, `.xml`, `.html`, `.css`, `.sh`

Максимальный размер файла: 10MB

## Архитектура

### RAG Pipeline
- **Инкрементальная индексация**: Файлы переиндексируются только при изменении (проверка по хешу SHA-256)
- **Чанкинг с overlap**: Текст разбивается на чанки с перекрытием для сохранения контекста
- **Семантический поиск**: Использует косинусное сходство векторов эмбеддингов (nomic-embed-text)
- **Реранкинг**: Опциональная переоценка релевантности результатов через LLM для повышения точности
- **Фильтрация по порогу**: Исключение нерелевантных результатов на основе оценки релевантности
- **LLM генерация**: Формирование ответов на основе контекста (llama3.1:8b)

### 🆕 MCP (Model Context Protocol)
- **JSON-based CRM**: Данные пользователей и тикетов хранятся в `data/crm_data.json`
- **Контекст пользователя**: Автоматическая загрузка активных и решенных тикетов
- **Персонализация**: LLM учитывает контекст при формировании ответов
- **Кэширование**: Данные CRM кэшируются на 1 минуту для оптимизации производительности

### База данных (SQLite)
- **Индексация**: documents, chunks, embeddings
- **История**: conversations, messages
- **Каскадное удаление**: Автоматическая очистка связанных данных

## Структура базы данных

### Таблицы индексации

- `documents`: Метаданные файлов
- `chunks`: Текстовые чанки с позициями
- `embeddings`: Векторы эмбеддингов

### Таблицы истории диалогов

- `conversations`: Беседы (сессии диалога)
  - `id`: Уникальный идентификатор беседы
  - `created_at`: Время создания беседы
  - `updated_at`: Время последнего обновления беседы

- `messages`: Сообщения в беседах
  - `id`: Уникальный идентификатор сообщения
  - `conversation_id`: Ссылка на беседу
  - `role`: Роль отправителя (`user` или `assistant`)
  - `content`: Текст сообщения
  - `mode`: Режим запроса (`plain` или `rag`)
  - `sources_json`: JSON с информацией об источниках (только для RAG режима)
  - `created_at`: Время создания сообщения

### 🆕 Данные CRM (MCP)

Файл `data/crm_data.json` содержит:

- **users**: Информация о пользователях
  - `id`, `name`, `email`, `role`, `organization`, `subscription`
  
- **tickets**: Тикеты поддержки
  - `id`, `user_id`, `title`, `description`
  - `status`: open, in_progress, resolved, closed
  - `priority`: critical, high, medium, low
  - `category`: authentication, search, indexing, configuration, etc.
  - `tags`, `resolution` (для решенных тикетов)

**Доступные пользователи для тестирования:**
- `user_001`: Нора Веденеева (Руководитель проекта)
- `user_002`: Мария Летова (Архивист-куратор)
- `user_003`: Илья Громин (Инженер инфраструктуры) - имеет активные тикеты
- `user_004`: Тимур Сафин (Юрист проекта)
- `user_005`: Оксана Рунова (Специалист по интерфейсам)
- `user_006`: Пётр Каплин (Кладовщик)

**Примечание:** Все таблицы БД и CRM данные создаются автоматически при первом запуске приложения.

## 📚 Документация

- **[SETUP_AND_USAGE.md](SETUP_AND_USAGE.md)** - Подробная инструкция по установке, настройке и использованию
- **[DEMO_EXAMPLES.md](DEMO_EXAMPLES.md)** - Демонстрационные примеры с пояснениями "что происходит под капотом"
- **README.md** (этот файл) - Краткий обзор и справочник по командам

## 🎯 Результат выполнения задания

Создан полнофункциональный **ассистент для поддержки пользователей**, который:

✅ **RAG система** - отвечает на вопросы используя документацию и FAQ:
  - 9 документов о проекте Соната-2041
  - 5 FAQ документов по разным темам
  - Семантический поиск с реранкингом
  - Автоматическое указание источников

✅ **MCP интеграция** - работает с данными из JSON (имитация CRM):
  - 6 пользователей с разными ролями
  - 10 тикетов поддержки (открытые, в работе, решенные)
  - Автоматическая загрузка контекста по user-id

✅ **Персонализированные ответы** - учитывает контекст пользователя:
  - Активные тикеты пользователя
  - Историю решенных проблем
  - Роль и организацию пользователя
  - Специфику проблемы из описания тикета

✅ **Пример использования:**
```bash
# Пользователь Илья Громин спрашивает про авторизацию
# У него есть открытый тикет #ticket_001 по этой проблеме
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация?'

# Ассистент:
# - Видит активный тикет пользователя
# - Находит информацию в FAQ про авторизацию
# - Формирует персонализированный ответ:
#   "Илья, вижу что у вас открыт тикет #ticket_001 по этой проблеме..."
```

