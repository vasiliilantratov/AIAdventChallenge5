# Демонстрационные примеры - Ассистент поддержки пользователей

## 🎯 Примеры использования системы

Этот документ содержит конкретные примеры использования ассистента с пояснениями что происходит "под капотом".

---

## Пример 1: Вопрос с контекстом активного тикета

### Сценарий
Илья Громин (user_003) - инженер инфраструктуры - имеет открытый тикет про проблему с авторизацией.

### Команда
```bash
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация?'
```

### Что происходит

1. **Загрузка контекста из MCP:**
   ```
   MCP Service читает data/crm_data.json
   → Находит пользователя user_003
   → Загружает активные тикеты:
     - ticket_001: "Не работает авторизация" (open, high)
     - ticket_007: "Сервер перегревается" (in_progress, critical)
   → Загружает недавние решенные тикеты (если есть)
   ```

2. **Вывод информации о пользователе:**
   ```
   👤 Пользователь: Илья Громин (Инженер инфраструктуры)
   📧 Email: i.gromin@sevkvarts.ru
   🎫 Активных тикетов: 2
   ```

3. **RAG Pipeline:**
   ```
   Вопрос → Эмбеддинг → Семантический поиск
   → Находит релевантные чанки из:
     - FAQ_01_Авторизация_и_доступ.txt
     - 01_Обзор_проекта_Соната_2041.txt
   ```

4. **Формирование промпта для LLM:**
   ```
   System Prompt:
   "Ты — ассистент службы поддержки проекта Соната-2041.
    Учитывай контекст пользователя (активные тикеты) при ответе.
    У пользователя есть активный тикет #ticket_001 по этой проблеме..."
   
   User Message:
   === КОНТЕКСТ ПОЛЬЗОВАТЕЛЯ ===
   Пользователь: Илья Громин
   Активные тикеты:
     - ticket_001: Не работает авторизация в системе Соната
       Описание: При попытке входа получаю ошибку...
   
   Вопрос: Почему не работает авторизация?
   
   Документация и FAQ:
   [найденные чанки из FAQ про авторизацию]
   ```

5. **Ответ LLM:**
   ```
   Илья, вижу что у вас открыт тикет #ticket_001 по этой проблеме.
   
   На основе документации, проблемы с авторизацией могут быть связаны с:
   
   1. Проверка роли пользователя
      - Убедитесь что у вас есть роль "Соната/Внутренний"
      - Проверить можно у руководителя проекта Норы Веденеевой
   
   2. Сброс пароля
      - Используйте функцию восстановления пароля
      - Новый пароль должен содержать минимум 8 символов...
   
   3. Кэш браузера
      - Попробуйте очистить кэш
      - Или используйте режим инкогнито
   
   Если эти шаги не помогут, рекомендую обновить тикет с результатами
   проверок, чтобы команда поддержки могла помочь более точно.
   
   Источники:
     1. FAQ_01_Авторизация_и_доступ.txt (text/plain)
        Путь: /home/vas/.../FAQ_01_Авторизация_и_доступ.txt
   ```

---

## Пример 2: Вопрос с использованием решенных тикетов

### Сценарий
Оксана Рунова (user_005) ранее решала проблему с Ollama (ticket_009 - resolved). Теперь задает похожий вопрос.

### Команда
```bash
./gradlew run --args='qa --mode rag --user-id user_005 Не могу подключиться к Ollama'
```

### Что происходит

1. **MCP загружает контекст:**
   ```
   Пользователь: user_005
   Активные тикеты:
     - ticket_003: Как настроить реранкинг (open, medium)
   
   Недавние решенные тикеты:
     - ticket_009: Ошибка подключения к Ollama (resolved)
       Решение: "Проблема была в файрволе. После добавления
                 правила для порта 11434 все заработало."
   ```

2. **LLM видит предыдущее решение:**
   ```
   System Prompt:
   "Если похожая проблема уже решалась ранее, 
    используй это решение как основу для ответа."
   
   Контекст:
   Недавние решенные тикеты:
     - ticket_009: Ошибка подключения к Ollama
       Решение: Проблема была в файрволе...
   ```

3. **Ответ:**
   ```
   Оксана, вижу что вы уже сталкивались с похожей проблемой (тикет #ticket_009).
   
   Тогда проблема решилась добавлением правила в файрвол для порта 11434.
   
   Рекомендую проверить:
   
   1. Ollama запущен:
      ollama list
   
   2. Порт 11434 открыт в файрволе:
      sudo ufw status
      sudo ufw allow 11434
   
   3. Ollama доступен:
      curl http://localhost:11434/api/tags
   
   Если эти шаги не помогут, возможно потребуется перезапустить Ollama.
   ```

---

## Пример 3: Критическая проблема инфраструктуры

### Сценарий
Илья Громин имеет критический тикет про перегрев сервера Лира-2.

### Команда
```bash
./gradlew run --args='qa --mode rag --user-id user_003 --enable-reranking Что делать с перегревом сервера Лира-2?'
```

### Что происходит

1. **MCP загружает контекст:**
   ```
   Активные тикеты:
     - ticket_007: Сервер Лира-2 перегревается (in_progress, CRITICAL!)
       Описание: При индексации температура поднимается до критических значений
       Назначен: infrastructure_team
   ```

2. **RAG с реранкингом:**
   ```
   Этап 1: Семантический поиск (rerank-top-k=10)
   → Находит 10 потенциально релевантных чанков
   
   Этап 2: Реранкинг через LLM
   → LLM оценивает каждый чанк:
     - 07_Отчет_об_инциденте_Лира2_температура.txt: 0.95
     - 01_Обзор_проекта_Соната_2041.txt: 0.72
     - FAQ_04_Конфигурация_и_настройка.txt: 0.68
     - ...
   
   Этап 3: Берем топ-5 результатов
   ```

3. **Ответ:**
   ```
   Илья, вижу что у вас критический тикет #ticket_007 по этой проблеме,
   который уже находится в работе у команды инфраструктуры.
   
   На основе отчета об инциденте и документации:
   
   Сервер Лира-2 (корпус Б, подвал, стойка R5) известен проблемами
   с перегревом при больших нагрузках.
   
   Рекомендации:
   
   1. СРОЧНО: Проверьте текущую температуру
      - Критический порог: 85°C
      - Рабочая норма: 60-70°C
   
   2. Временное решение:
      - Снизьте нагрузку (приостановите индексацию)
      - Проверьте работу системы охлаждения
   
   3. Долгосрочное решение:
      - Оптимизация процесса индексации (батчинг)
      - Улучшение вентиляции в помещении
      - Возможно потребуется апгрейд охлаждения
   
   Учитывая критичность проблемы и то что тикет уже в работе,
   рекомендую связаться с командой напрямую для координации действий.
   
   Источники:
     1. 07_Отчет_об_инциденте_Лира2_температура.txt
     2. 01_Обзор_проекта_Соната_2041.txt
   ```

---

## Пример 4: Вопрос без контекста пользователя

### Сценарий
Вопрос задается без указания user-id (анонимный пользователь).

### Команда
```bash
./gradlew run --args='qa --mode rag Как работает семантический поиск?'
```

### Что происходит

1. **Без MCP контекста:**
   ```
   user-id не указан
   → MCP не используется
   → Контекст пользователя отсутствует
   → Обычный RAG без персонализации
   ```

2. **RAG Pipeline:**
   ```
   Семантический поиск по FAQ
   → FAQ_03_Поиск_и_RAG.txt
   → 01_Обзор_проекта_Соната_2041.txt
   ```

3. **Ответ (общий, без персонализации):**
   ```
   Семантический поиск в Соната-2041 использует векторные эмбеддинги
   для нахождения документов по смыслу:
   
   1. Запрос преобразуется в вектор (модель nomic-embed-text)
   2. Вычисляется косинусное сходство с векторами чанков
   3. Возвращаются наиболее похожие результаты
   
   Это позволяет находить документы даже если они используют синонимы
   или другие формулировки. Например, запрос "как удалить файл" найдет
   документы про "удаление документов" или "очистку индекса".
   
   Источники:
     1. FAQ_03_Поиск_и_RAG.txt
   ```

---

## Пример 5: Полная конфигурация с реранкингом и фильтрацией

### Команда
```bash
./gradlew run --args='qa --mode rag \
  --user-id user_002 \
  --enable-reranking \
  --relevance-threshold 0.5 \
  --top-k 3 \
  --rerank-top-k 10 \
  Почему поиск не находит мои документы?'
```

### Параметры

- `--mode rag` - использовать RAG
- `--user-id user_002` - контекст Марии Летовой (архивист)
- `--enable-reranking` - включить реранкинг
- `--relevance-threshold 0.5` - порог релевантности 0.5
- `--top-k 3` - вернуть 3 лучших результата
- `--rerank-top-k 10` - взять 10 результатов для реранкинга

### Процесс обработки

```
1. MCP: Загрузка контекста user_002
   → Активный тикет #ticket_002: "Поиск не находит документы"
   
2. Семантический поиск (rerank-top-k=10)
   → 10 результатов с similarity scores
   
3. Реранкинг через LLM
   → LLM оценивает каждый результат (0.0-1.0)
   → Сортировка по rerankScore
   
4. Фильтрация (threshold=0.5)
   → Отсеиваем результаты с score < 0.5
   → Остается, например, 6 результатов
   
5. Топ-K (k=3)
   → Берем 3 лучших результата
   
6. Генерация ответа
   → С учетом контекста пользователя и активного тикета

Статистика:
  Начальных результатов: 10
  После реранкинга: 10
  После фильтрации: 6
  Финальных результатов: 3
```

### Ответ

```
Мария, вижу что у вас открыт тикет #ticket_002 по этой проблеме,
который уже в работе.

На основе документации, причины могут быть следующими:

1. Проверьте путь к БД
   - Команды index и search должны использовать один --db-path
   - По умолчанию: ./index.db

2. Проверьте что документы проиндексированы
   ./gradlew run --args="stats"
   → Должны быть: Documents > 0, Embeddings > 0

3. Эмбеддинги не созданы
   - Если Embeddings = 0, значит Ollama был недоступен при индексации
   - Решение: Переиндексируйте документы

Источники:
  1. FAQ_02_Индексация_документов.txt
  2. FAQ_03_Поиск_и_RAG.txt
  3. FAQ_04_Конфигурация_и_настройка.txt
```

---

## Пример 6: Вопрос о проекте Соната с контекстом

### Команда
```bash
./gradlew run --args='qa --mode rag --user-id user_001 Где находится Мнемоключ МК-7?'
```

### Особенности

user_001 - Нора Веденеева (руководитель проекта) - имеет подписку "enterprise" и доступ ко всей информации.

### Ответ

```
Мнемоключ МК-7 — это аппаратный маркер сеансов нормализации и подписи
набора документов в проекте Соната-2041.

Технические характеристики:
- Серийный номер: 7-041-118
- Назначение: Маркер сеансов нормализации
- Использование: Подпись наборов документов

Местоположение: [информация из документов проекта]

Мнемоключ является частью системы безопасности и контроля версий
документации в архивном комплексе "Северная Вышка" (г. Тарск).

Источники:
  1. 04_Техпаспорт_Мнемоключ_МК-7.txt
  2. 01_Обзор_проекта_Соната_2041.txt
```

---

## Сравнение: С контекстом vs Без контекста

### Вопрос: "Почему не работает авторизация?"

#### Без контекста (--user-id не указан)
```
Ответ: Общая информация из FAQ
- Проверьте учетные данные
- Убедитесь в правильности роли
- Очистите кэш браузера
- Попробуйте сбросить пароль
```

#### С контекстом (--user-id user_003)
```
Ответ: Персонализированный с учетом тикета
- "Илья, вижу ваш тикет #ticket_001"
- Конкретные рекомендации на основе описания проблемы
- Учет роли пользователя (инженер инфраструктуры)
- Предложение обновить тикет с результатами
```

---

## Статистика обработки

При использовании реранкинга система выводит статистику:

```
Статистика обработки:
  Начальных результатов: 10
  После предварительной фильтрации: 8
  После реранкинга: 8
  После фильтрации по порогу: 5
  Финальных результатов: 3
```

Это помогает понять как работает pipeline и настроить параметры.

---

## Доступные пользователи для тестирования

```
user_001: Нора Веденеева (Руководитель проекта)
  - Тикеты: ticket_006 (resolved), ticket_010 (open)

user_002: Мария Летова (Архивист-куратор)
  - Тикеты: ticket_002 (in_progress), ticket_008 (open)

user_003: Илья Громин (Инженер инфраструктуры)
  - Тикеты: ticket_001 (open, high), ticket_007 (in_progress, critical)

user_004: Тимур Сафин (Юрист проекта)
  - Тикеты: ticket_005 (open, medium)

user_005: Оксана Рунова (Специалист по интерфейсам)
  - Тикеты: ticket_003 (open), ticket_009 (resolved)

user_006: Пётр Каплин (Кладовщик)
  - Тикеты: ticket_004 (resolved)
```

---

**Примечание:** Для полноценной работы всех примеров убедитесь что:
1. Ollama запущен (`ollama serve`)
2. Модели установлены (`ollama pull nomic-embed-text:latest` и `llama3.1:8b`)
3. Документы проиндексированы (`./gradlew run --args="index data"`)
