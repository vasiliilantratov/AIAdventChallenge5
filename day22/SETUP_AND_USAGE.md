# Ассистент для поддержки пользователей - Инструкция по настройке и использованию

## 📋 Описание проекта

Это мини-сервис ассистента для поддержки пользователей проекта Соната-2041, который:
- Отвечает на вопросы пользователей о продукте используя **RAG** (Retrieval-Augmented Generation)
- Использует документацию и FAQ для формирования ответов
- Интегрирован с **MCP** (Model Context Protocol) для работы с данными пользователей и тикетами из CRM системы
- Учитывает контекст пользователя (активные тикеты, историю обращений) при ответах

## 🎯 Ключевые возможности

### 1. RAG (Retrieval-Augmented Generation)
- Семантический поиск по документации и FAQ
- Генерация ответов на основе найденных документов
- Реранкинг результатов для повышения точности
- Фильтрация по порогу релевантности

### 2. MCP (Model Context Protocol)
- Чтение данных пользователей из JSON-файла (имитация CRM)
- Автоматическая загрузка контекста пользователя по user-id
- Учет активных и решенных тикетов при ответах
- Персонализированные ответы с учетом истории пользователя

### 3. Поддерживаемые документы
- 9 документов о проекте Соната-2041
- 5 FAQ документов по разным темам:
  - Авторизация и доступ
  - Индексация документов
  - Поиск и RAG
  - Конфигурация и настройка
  - Поддержка пользователей

## 🚀 Установка и настройка

### Шаг 1: Установите необходимое ПО

**Требования:**
- Java 21+
- Gradle (включен в проект)
- Ollama

### Шаг 2: Установите Ollama и модели

```bash
# Установка Ollama (если еще не установлен)
# Linux:
curl -fsSL https://ollama.com/install.sh | sh

# macOS:
brew install ollama

# После установки запустите Ollama
ollama serve
```

**В новом терминале установите модели:**

```bash
# Модель для эмбеддингов
ollama pull nomic-embed-text:latest

# Модель для генерации ответов
ollama pull llama3.1:8b
```

### Шаг 3: Соберите проект

```bash
cd /home/vas/Documents/Projects/AIAdventChallenge5/day22
./gradlew build
```

### Шаг 4: Индексируйте документы

```bash
# Очистите старый индекс (если есть)
./gradlew run --args="clear"

# Проиндексируйте документы и FAQ
./gradlew run --args="index data --chunk-size 512 --overlap 50"

# Проверьте статистику
./gradlew run --args="stats"
```

Ожидаемый результат:
```
Index Statistics:
  Documents: 15
  Chunks: ~100+
  Embeddings: ~100+
```

## 📚 Использование

### Базовые команды

#### 1. Вопрос без RAG (только LLM)
```bash
./gradlew run --args='qa --mode plain Что такое Kotlin?'
```

#### 2. Вопрос с RAG (используя документацию)
```bash
./gradlew run --args='qa --mode rag Что такое проект Соната-2041?'
```

#### 3. Вопрос с RAG и реранкингом
```bash
./gradlew run --args='qa --mode rag --enable-reranking Как настроить реранкинг?'
```

### Использование MCP (контекст пользователя)

#### Пример 1: Вопрос от пользователя с активным тикетом
```bash
# Пользователь user_003 (Илья Громин) имеет активный тикет про авторизацию
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация?'
```

**Что происходит:**
1. Система загружает контекст пользователя из `data/crm_data.json`
2. Видит активный тикет #ticket_001 "Не работает авторизация в системе Соната"
3. Находит релевантную информацию в FAQ
4. Формирует ответ с учетом контекста тикета

**Пример вывода:**
```
👤 Пользователь: Илья Громин (Инженер инфраструктуры)
📧 Email: i.gromin@sevkvarts.ru
🎫 Активных тикетов: 2

Режим: с RAG
  контекст пользователя: включен

Вопрос:
Почему не работает авторизация?

Ответ:
Илья, вижу что у вас открыт тикет #ticket_001 по этой проблеме. 
На основе документации, проблемы с авторизацией могут быть связаны с:

1. Проверьте роль пользователя - нужна роль "Соната/Внутренний"
2. Попробуйте очистить кэш браузера
3. Убедитесь что сессия не истекла (действует 8 часов)

Если проблема сохраняется, рекомендую обратиться к руководителю проекта...

Источники:
  1. FAQ_01_Авторизация_и_доступ.txt
```

#### Пример 2: Вопрос от пользователя с решенными тикетами
```bash
# Пользователь user_005 (Оксана Рунова) имеет решенный тикет про Ollama
./gradlew run --args='qa --mode rag --user-id user_005 --enable-reranking Не могу подключиться к Ollama'
```

**Что происходит:**
1. Система видит что похожая проблема уже была решена (тикет #ticket_009)
2. Использует решение из истории
3. Дополняет информацией из FAQ

#### Пример 3: Новый пользователь без контекста
```bash
# Пользователь user_004 (Тимур Сафин) задает общий вопрос
./gradlew run --args='qa --mode rag --user-id user_004 Как удалить документ из индекса?'
```

### Продвинутое использование

#### С настройкой всех параметров
```bash
./gradlew run --args='qa --mode rag \
  --user-id user_003 \
  --enable-reranking \
  --relevance-threshold 0.4 \
  --top-k 5 \
  --rerank-top-k 10 \
  Почему перегревается сервер Лира-2?'
```

#### Просмотр истории диалогов
```bash
# Список всех бесед
./gradlew run --args="history"

# Конкретная беседа
./gradlew run --args="history --conversation-id 1 --format full"
```

## 📊 Структура данных CRM

Файл `data/crm_data.json` содержит:

### Пользователи (users)
```json
{
  "id": "user_001",
  "name": "Нора Веденеева",
  "email": "n.vedeneeva@sevkvarts.ru",
  "role": "Руководитель проекта",
  "organization": "АО СевКварц",
  "subscription": "enterprise"
}
```

### Тикеты (tickets)
```json
{
  "id": "ticket_001",
  "user_id": "user_003",
  "title": "Не работает авторизация в системе Соната",
  "description": "...",
  "status": "open",
  "priority": "high",
  "category": "authentication",
  "tags": ["авторизация", "доступ"]
}
```

### Статусы тикетов
- **open** - открыт, ожидает обработки
- **in_progress** - в работе
- **resolved** - решен
- **closed** - закрыт

### Приоритеты
- **critical** - критический (ответ в течение 2 часов)
- **high** - высокий (ответ в течение 4 часов)
- **medium** - средний (ответ в течение 1 дня)
- **low** - низкий (ответ в течение 3 дней)

## 🎭 Демонстрационные сценарии

### Сценарий 1: Пользователь с проблемой авторизации
```bash
# Шаг 1: Пользователь создает тикет (уже есть в crm_data.json)
# user_003 имеет открытый тикет #ticket_001 про авторизацию

# Шаг 2: Пользователь задает вопрос ассистенту
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация в системе Соната?'

# Ассистент:
# 1. Видит активный тикет пользователя
# 2. Находит информацию в FAQ_01_Авторизация_и_доступ.txt
# 3. Формирует персонализированный ответ с учетом контекста
```

### Сценарий 2: Повторяющаяся проблема
```bash
# user_005 уже решала проблему с Ollama (ticket_009 - resolved)
# Теперь похожая проблема у другого пользователя

./gradlew run --args='qa --mode rag --user-id user_001 Ошибка подключения к Ollama'

# Ассистент может найти похожие решенные тикеты через MCP
# (функция searchSimilarTickets в McpService)
```

### Сценарий 3: Вопрос о конфигурации
```bash
# Пользователь хочет настроить систему
./gradlew run --args='qa --mode rag --user-id user_005 --enable-reranking Как настроить порог релевантности?'

# Ассистент:
# 1. Находит информацию в FAQ_03_Поиск_и_RAG.txt
# 2. Дает подробный ответ с примерами
# 3. Может упомянуть открытый тикет user_005 по настройке
```

### Сценарий 4: Критическая проблема инфраструктуры
```bash
# user_003 (инженер) имеет критический тикет про перегрев сервера
./gradlew run --args='qa --mode rag --user-id user_003 Что делать с перегревом сервера Лира-2?'

# Ассистент:
# 1. Видит критический тикет #ticket_007
# 2. Находит информацию о сервере Лира-2 в документах проекта
# 3. Может предложить связаться со специалистом (так как тикет в работе)
```

## 🔧 Архитектура решения

### Компоненты системы

1. **RAG Pipeline:**
   - Индексация документов с chunking
   - Векторные эмбеддинги (nomic-embed-text)
   - Семантический поиск (косинусное сходство)
   - Опциональный реранкинг через LLM
   - Генерация ответов (llama3.1:8b)

2. **MCP Integration:**
   - `McpService` - сервис для работы с CRM данными
   - `McpModels` - модели данных (User, Ticket, UserContext)
   - Загрузка контекста пользователя по user-id
   - Форматирование контекста для LLM

3. **CLI Interface:**
   - Команда `qa` с параметром `--user-id`
   - Автоматическая загрузка контекста
   - Вывод информации о пользователе и тикетах

4. **Database:**
   - SQLite для хранения индекса
   - Таблицы: documents, chunks, embeddings
   - История диалогов: conversations, messages

### Поток данных

```
Вопрос пользователя
    ↓
[CLI] Загрузка user-id → [MCP] Получение контекста из JSON
    ↓
[RAG] Семантический поиск по FAQ/документации
    ↓
[Reranker] Опциональная переоценка релевантности
    ↓
[LLM] Генерация ответа с учетом:
    - Контекста пользователя (тикеты, история)
    - Найденных документов (FAQ, проектная документация)
    ↓
Персонализированный ответ
```

## 📝 Примеры вопросов

### Вопросы по авторизации
```bash
./gradlew run --args='qa --mode rag --user-id user_003 Почему не работает авторизация?'
./gradlew run --args='qa --mode rag --user-id user_004 Как получить доступ к системе?'
./gradlew run --args='qa --mode rag Что делать если система не принимает пароль?'
```

### Вопросы по индексации
```bash
./gradlew run --args='qa --mode rag --user-id user_002 Поиск не находит документы'
./gradlew run --args='qa --mode rag --user-id user_006 Ошибка при индексации больших файлов'
./gradlew run --args='qa --mode rag Как работает инкрементальная индексация?'
```

### Вопросы по конфигурации
```bash
./gradlew run --args='qa --mode rag --user-id user_005 Как настроить реранкинг?'
./gradlew run --args='qa --mode rag Ошибка подключения к Ollama'
./gradlew run --args='qa --mode rag Как изменить модель LLM?'
```

### Вопросы о проекте Соната
```bash
./gradlew run --args='qa --mode rag Что такое проект Соната-2041?'
./gradlew run --args='qa --mode rag Где находится сервер Лира-2?'
./gradlew run --args='qa --mode rag Что такое Мнемоключ МК-7?'
./gradlew run --args='qa --mode rag Что такое кварцевый шёпот?'
```

## 🐛 Решение проблем

### Проблема: Ollama не запущен
```
Ошибка: Connection refused
Решение: Запустите Ollama в отдельном терминале: ollama serve
```

### Проблема: Модели не установлены
```
Ошибка: Model not found
Решение: 
  ollama pull nomic-embed-text:latest
  ollama pull llama3.1:8b
```

### Проблема: Embeddings = 0
```
Причина: Ollama не был запущен при индексации
Решение:
  1. Запустите ollama serve
  2. Очистите индекс: ./gradlew run --args="clear"
  3. Переиндексируйте: ./gradlew run --args="index data"
```

### Проблема: Пользователь не найден
```
Ошибка: User with ID 'xxx' not found
Решение: Проверьте user-id в файле data/crm_data.json
Доступные ID: user_001, user_002, user_003, user_004, user_005, user_006
```

## 📈 Метрики и мониторинг

### Логи
- **LLM запросы:** `llm-requests.log` (в корне проекта)
- **Консоль:** Gradle выводит все логи в реальном времени

### Статистика
```bash
./gradlew run --args="stats"
```

### История диалогов
```bash
./gradlew run --args="history"
```

## 🔐 Безопасность

- Данные CRM хранятся локально в JSON
- Доступ к системе только через CLI (нет внешнего API)
- SQLite база данных защищена правами файловой системы
- Логи LLM могут содержать чувствительные данные - храните их безопасно

## 🎓 Дополнительно

### Расширение CRM данных
Отредактируйте `data/crm_data.json`:
- Добавьте новых пользователей в массив `users`
- Добавьте новые тикеты в массив `tickets`
- Система автоматически загрузит обновленные данные

### Добавление новых FAQ
1. Создайте файл `data/FAQ_06_Ваша_тема.txt`
2. Переиндексируйте: `./gradlew run --args="index data"`
3. Новые FAQ будут доступны в RAG

### Интеграция с реальной CRM
Замените `McpService` чтобы читать данные из:
- REST API вашей CRM
- База данных (PostgreSQL, MySQL)
- Другие источники

## 📞 Контакты

**Руководитель проекта:** Нора Веденеева (n.vedeneeva@sevkvarts.ru)  
**Техническая поддержка:** support@sevkvarts.ru

---

**Версия:** 1.0  
**Дата:** 2026-01-14
