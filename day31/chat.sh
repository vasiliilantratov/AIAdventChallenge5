#!/bin/bash

# Скрипт для запуска интерактивного чата
# Использует installDist для корректной работы stdin

cd "$(dirname "$0")"

echo "🔨 Проверка сборки..."
./gradlew installDist -q

if [ $? -eq 0 ]; then
    echo "✅ Сборка готова"
    echo ""
    ./build/install/day24/bin/day24 chat "$@"
else
    echo "❌ Ошибка при сборке"
    exit 1
fi
