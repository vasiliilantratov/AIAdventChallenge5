# Настройка голосового агента (Speech → LLM → Text)

## Требования

- Java 21+
- Gradle
- Ollama с установленной моделью
- Микрофон
- Модель Vosk для распознавания речи

## Установка модели Vosk

### 1. Скачайте модель для русского языка

Перейдите на официальный сайт: https://alphacephei.com/vosk/models

Выберите одну из моделей для русского языка:

#### Маленькая модель (рекомендуется для тестирования)
- **Название**: vosk-model-small-ru-0.22
- **Размер**: ~45 MB
- **Скорость**: быстрая
- **Точность**: средняя
- **Ссылка**: https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip

#### Большая модель (рекомендуется для продакшена)
- **Название**: vosk-model-ru-0.42
- **Размер**: ~1.5 GB
- **Скорость**: медленная
- **Точность**: высокая
- **Ссылка**: https://alphacephei.com/vosk/models/vosk-model-ru-0.42.zip

### 2. Распакуйте и установите модель

```bash
# Скачайте модель (пример с маленькой моделью)
cd /home/vas/Documents/Projects/AIAdventChallenge5/day31
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip

# Распакуйте архив
unzip vosk-model-small-ru-0.22.zip

# Переименуйте папку для удобства
mv vosk-model-small-ru-0.22 vosk-model

# Удалите архив (опционально)
rm vosk-model-small-ru-0.22.zip
```

Структура должна выглядеть так:
```
day31/
├── vosk-model/
│   ├── am/
│   ├── graph/
│   ├── conf/
│   └── ...
├── src/
├── build.gradle.kts
└── ...
```

### 3. Проверка микрофона

#### Linux
```bash
# Проверить, что микрофон доступен
arecord -l

# Записать тестовое аудио (5 секунд)
arecord -d 5 -f cd test.wav

# Воспроизвести тестовое аудио
aplay test.wav
```

#### Права доступа (если нужно)
```bash
# Добавить пользователя в группу audio
sudo usermod -a -G audio $USER

# Перелогиниться для применения изменений
```

## Использование

### Запуск голосового агента

```bash
./gradlew run --args="voice"
```

### Режимы работы

После запуска доступны следующие опции:

1. **🎤 Голосовой ввод** - говорите в микрофон, нажмите Enter для остановки
2. **⌨️ Текстовый ввод** - введите текст вручную (для тестирования без микрофона)
3. **🧪 Тестовые запросы** - готовые примеры:
   - "Посчитай 25 * 34"
   - "Дай определение нейронной сети"
   - "Расскажи короткий анекдот"
4. **📜 История** - просмотр истории диалогов
5. **🔄 Новая беседа** - начать новую сессию
6. **🚪 Выход** - завершить работу

### Параметры командной строки

```bash
# С указанием пути к модели Vosk
./gradlew run --args="voice --vosk-model ./my-vosk-model"

# С указанием URL Ollama
./gradlew run --args="voice --ollama-url http://localhost:11434"

# С указанием пути к базе данных
./gradlew run --args="voice --db-path ./my-database.db"
```

## Примеры использования

### Пример 1: Вычисления
```
👤 Вы (голос): "Посчитай двадцать пять умножить на тридцать четыре"
📝 Распознанный текст: "посчитай двадцать пять умножить на тридцать четыре"

🤖 Ассистент:
25 × 34 = 850
```

### Пример 2: Определения
```
👤 Вы (голос): "Что такое нейронная сеть?"
📝 Распознанный текст: "что такое нейронная сеть"

🤖 Ассистент:
Нейронная сеть — это математическая модель, которая имитирует работу
человеческого мозга. Она состоит из искусственных нейронов, соединенных
между собой, и способна обучаться на данных для решения различных задач,
таких как распознавание образов или прогнозирование.
```

### Пример 3: Развлечения
```
👤 Вы (голос): "Расскажи анекдот"
📝 Распознанный текст: "расскажи анекдот"

🤖 Ассистент:
Программист ложится спать. Жена говорит: "Дорогой, купи по дороге хлеб,
если будут яйца — возьми десяток". Программист приходит с 10 батонами хлеба.
Жена: "Зачем столько?" Программист: "Так яйца же были!"
```

## Отладка и устранение проблем

### Проблема: "Модель Vosk не найдена"

**Решение:**
1. Убедитесь, что папка `vosk-model` существует в корне проекта
2. Проверьте структуру папки (должны быть подпапки `am`, `graph`, `conf`)
3. Укажите правильный путь через параметр `--vosk-model`

### Проблема: "Микрофон недоступен"

**Решение:**
1. Проверьте подключение микрофона: `arecord -l`
2. Проверьте права доступа (см. раздел "Права доступа")
3. Закройте другие приложения, использующие микрофон
4. Используйте текстовый режим для тестирования работы LLM

### Проблема: "Речь не распознается"

**Решение:**
1. Говорите четко и громко
2. Уменьшите фоновый шум
3. Попробуйте использовать большую модель (более точная)
4. Проверьте качество микрофона
5. Настройте громкость микрофона в системе

### Проблема: "Ollama не отвечает"

**Решение:**
1. Убедитесь, что Ollama запущена: `ollama serve`
2. Проверьте доступность: `curl http://localhost:11434/api/tags`
3. Установите модель: `ollama pull llama3.1:8b`

## Технические детали

### Архитектура

```
┌─────────────┐
│  Микрофон   │
└──────┬──────┘
       │ Audio (16kHz, mono)
       ▼
┌─────────────────────┐
│ SpeechRecognition   │
│ Service (Vosk)      │
└──────┬──────────────┘
       │ Text
       ▼
┌─────────────────────┐
│ VoiceCommand        │
└──────┬──────────────┘
       │ Prompt + Text
       ▼
┌─────────────────────┐
│ OllamaLlmService    │
└──────┬──────────────┘
       │ Answer (Text)
       ▼
┌─────────────────────┐
│ Console Output      │
└─────────────────────┘
```

### Характеристики

- **Формат аудио**: 16 kHz, 16 bit, mono
- **Размер буфера**: 4096 байт
- **Движок распознавания**: Vosk (офлайн)
- **LLM**: Ollama (llama3.1:8b по умолчанию)
- **Хранение истории**: SQLite

### Зависимости

- `com.alphacephei:vosk:0.3.45` - распознавание речи
- `javax.sound.sampled` - захват аудио с микрофона
- `io.ktor:ktor-client-*` - HTTP клиент для Ollama
- `org.jetbrains.exposed:*` - ORM для SQLite

## Демонстрация для задания

Для выполнения задания "Голосовой агент (Speech → LLM → Text)" выполните:

### 1. Подготовка
```bash
# Установка модели Vosk
wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
unzip vosk-model-small-ru-0.22.zip
mv vosk-model-small-ru-0.22 vosk-model

# Проверка микрофона
arecord -l
```

### 2. Запуск
```bash
./gradlew run --args="voice"
```

### 3. Тестирование запросов
- **Вычисление**: "Посчитай двадцать пять умножить на тридцать четыре"
- **Определение**: "Что такое искусственный интеллект?"
- **Анекдот**: "Расскажи анекдот про программистов"

### 4. Запись видео
Запишите экран с демонстрацией:
1. Запуск приложения
2. Выбор голосового ввода
3. Произнесение 3 тестовых запросов
4. Получение текстовых ответов от LLM

### 5. Результат
- ✅ Речь распознается в текст
- ✅ Текст отправляется в LLM
- ✅ Ответ возвращается текстом
- ✅ История сохраняется в БД

## Дополнительная информация

### Поддерживаемые языки (Vosk)

Vosk поддерживает более 20 языков. Модели доступны на:
https://alphacephei.com/vosk/models

Для смены языка:
1. Скачайте модель для нужного языка
2. Распакуйте в отдельную папку
3. Запустите с параметром `--vosk-model ./vosk-model-en`

### Альтернативы Vosk

- **Whisper (OpenAI)** - более точный, требует GPU
- **Google Speech-to-Text** - облачный, платный
- **DeepSpeech (Mozilla)** - офлайн, архивный проект

## Лицензии

- **Vosk**: Apache 2.0
- **Модели Vosk**: Apache 2.0
- **Ollama**: MIT
